export const meta = {
  name: 'residual-gap-closure',
  description: 'Close the residual lower-priority gaps: recruiter eligibility-rule authoring UI, onboarding document-upload endpoint, saga/admin-route tenant-body hardening (defense in depth, no login breakage), and RLS-list completeness across services. 4 disjoint lanes, write-only, additive; orchestrator builds/tests.',
  phases: [
    { title: 'Fix', detail: '4 disjoint lanes: eligibility-ui, onboarding-docs, tenant-hardening, rls-completeness' },
  ],
}

const REPORT = { type: "object", additionalProperties: false, properties: {
  files: { type: "array", items: { type: "string" } },
  summary: { type: "string" },
  schemaChanged: { type: "boolean" },
  notes: { type: "string", description: "anything the orchestrator must do (prisma regen, a cross-file follow-up) or any risk" },
}, required: ["files", "summary", "schemaChanged", "notes"] };

const CTX = "Repo D:/CDC/ATS - a SHIPPED multi-tenant ATS (Express microservices apps/*, Next.js apps/frontend, packages/common+contracts+nats-client, DB-per-service Postgres + RLS, NATS, MinIO, Docker static images). EXECUTION RULE: write SOURCE ONLY - no npm/npx/tsc/prisma/docker (orchestrator builds/regens prisma/tests; those pop a Windows dialog here). Services are NodeNext ESM (.js import specifiers). HARD RULES: everything ADDITIVE + backward-compatible (a frozen v1 image + current v2 must be unaffected; do NOT change existing route/response shapes in a breaking way - additive only); REAL data or honest empty states, never fabricated; match existing idioms; no em or en dashes in authored text; never log/commit secrets. Stay STRICTLY in your lane's file territory; DO NOT edit apps/frontend/lib/api.ts or apps/frontend/components/cd/types.ts or apps/frontend/lib/aurora-types.ts (shared files other lanes may touch) - add lane-local helpers/types in YOUR own new/existing files instead. READ the real code before editing.";

phase('Fix')
log('4 residual-gap lanes')
const fix = (await parallel([
  () => agent(CTX + "\n\n=== LANE A: ELIGIBILITY-RULE AUTHORING UI (territory: apps/job-service + apps/frontend requisition/job authoring screens ONLY - e.g. components/cd/RequisitionBuilder.tsx and the requisition builder/edit screen; NOT lib/api.ts) ===\nThe eligibility GATING ENGINE already works (job-service requisitions.ts + public.ts enforce a spec at apply time, generic/spec-driven not CSE-hardcoded). What is missing is a recruiter-facing UI to AUTHOR the eligibility spec on a job/requisition.\n1. First READ how the eligibility spec is stored + validated today (grep eligibility in apps/job-service/src/routes/requisitions.ts + the requisition/JobPosting prisma model + packages/contracts requisition DTOs). Determine the exact shape (department/degree/minCgpa/custom criteria).\n2. If the requisition create/update endpoint already accepts the eligibility spec, DO NOT change the backend - just build the UI. If the endpoint does NOT persist it, add it ADDITIVELY (optional field, backward-compatible) to the requisition route + DTO.\n3. Build the authoring UI: a section in the requisition builder/edit screen where the recruiter defines the eligibility rules (allowed departments multi-select, required degree, min CGPA, and a generic add-custom-criterion row), bound to the real requisition create/update API. Honest empty (no rules = open to all). Match the existing RequisitionBuilder form idiom. Add any API call in a lane-local file, not the shared lib/api.ts.\nReport whether a backend change was needed.", { label: 'fix:eligibility-ui', phase: 'Fix', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE B: ONBOARDING DOCUMENT-UPLOAD (territory: apps/onboarding-service + the frontend onboarding PORTAL page ONLY - app/(onboarding)/** or the onboarding portal route; NOT other frontend files) ===\nThe onboarding case seeds DOCUMENT tasks (e.g. upload PAN, bank proof) but there is NO endpoint for the new hire to upload the document, so those tasks cannot be completed.\n1. READ apps/onboarding-service (routes/portal.ts, case-service.ts, the OnboardingTask/OnboardingCase prisma models, how the portal authenticates the hire via the portalToken) + how resume-service stores uploads to MinIO (apps/resume-service storage/incoming-storage) for the idiom.\n2. Add a document-upload endpoint on the portal (token-authenticated, no login) that accepts a file for a specific DOCUMENT task and stores it (MinIO/S3 via the existing storage idiom, OR a presigned upload), records the object key + filename on the task, and marks the task submitted/complete. Multipart or presigned; if multipart on an RLS client, re-apply tenant context AFTER multer (known gotcha). Validate file type/size; never trust client-supplied size. If the OnboardingTask model needs an additive field for the stored doc (objectKey/filename/uploadedAt), add it (schemaChanged=true).\n3. Wire the onboarding portal UI: an upload control on each DOCUMENT task that calls the new endpoint and reflects completion. Honest states (pending/uploaded/failed). Real data only.\nReport schema changes for prisma regen.", { label: 'fix:onboarding-docs', phase: 'Fix', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE C: TENANT-BODY HARDENING (territory: apps/identity-service ONLY, plus a read of api-gateway to confirm the proxy path; do NOT edit gateway or common) ===\nDefense-in-depth for master prompt section 7 ('tenant context NEVER trusted from a client body field'). The audit flagged: the register-saga user-create route is reachable via the generic proxy, and admin-gated internal routes lack service-layer body-tenant hygiene.\nCRITICAL SAFETY: do NOT break login, register-saga (legitimate tenant+user bootstrap), or SSO. These pre-auth flows LEGITIMATELY carry tenant/user data in the body. Only HARDEN routes that ALREADY have a verified tenant context (X-Tenant-Id stamped by the gateway from the JWT).\n1. READ apps/identity-service routes (users.ts, the saga/register route, super-admin routes, sso) + how tenantId is derived (getTenantId from readAuthHeaders vs any req.body.tenantId usage). grep for `body.tenantId` / `req.body` tenant reads.\n2. For admin-gated INTERNAL routes that operate within the caller's tenant: ensure tenantId comes from the verified context (getTenantId), and if the body ALSO carries tenantId, either ignore it or reject a MISMATCH with a 400 (never let a body tenantId override the verified one). Additive guard; keep existing behavior when they agree.\n3. For the register-saga route reachable via the generic proxy: confirm it is intended to be pre-auth (it bootstraps a new tenant). If it is, add a clear comment + any input hardening (it should NOT be usable to write into an EXISTING tenant it is not authorized for) WITHOUT breaking new-tenant signup. If it should be internal-only, ensure the internal-service-token guard applies.\nBe conservative: prefer added assertions over changed flows. Report exactly what you hardened + confirm login/saga remain intact.", { label: 'fix:tenant-hardening', phase: 'Fix', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE D: RLS-LIST COMPLETENESS (territory: apps/*/prisma/apply-rls.ts across ALL services ONLY - do NOT touch prisma.ts, schema.prisma, or any request-path code) ===\nThe arch audit flagged incomplete RLS coverage. apply-rls.ts (per service) lists the tables that get ENABLE/FORCE ROW LEVEL SECURITY + the tenant policy. This runs ONLY in production (the demo has no ats_app role so apply-rls is inert) - so ADDING a per-tenant table to the list is SAFE + additive production hardening.\n1. For EACH service that has an apply-rls.ts (grep to find them: candidate/interview/screening/resume/job/billing/notification/identity/tenant), READ its apply-rls.ts table list (STRICT_TABLES / TENANT_TABLES) AND its prisma/schema.prisma models.\n2. For every model that HAS a `tenantId` column (tenant-scoped) but is NOT in that service's RLS list, ADD it to the list (+ ensure the standard tenant_isolation policy is applied to it, mirroring the existing entries). Respect existing exceptions the code documents (e.g. job Skill table is OR-tenant-is-null for global skills; Outbox intentionally un-RLS'd; NULL-tenant notification rows) - do NOT change those documented choices, just note them.\n3. Do NOT change the two-client setup, the request path, or add RLS where a table is intentionally cross-tenant/global. Purely extend the table lists to cover tenant-scoped models that were missed.\nReport, per service, which tables you ADDED and which you intentionally left out (with the reason).", { label: 'fix:rls-completeness', phase: 'Fix', schema: REPORT }),
])).filter(Boolean)
log('Fix done: ' + fix.length + '/4 lanes; schemaChanged in: ' + fix.filter(f => f.schemaChanged).map(f => f.files[0]).join(', '))

return { lanes: fix.map(f => ({ summary: f.summary, files: f.files.length, schemaChanged: f.schemaChanged, notes: f.notes })) }
