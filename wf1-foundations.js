export const meta = {
  name: 'wf1-shared-foundations',
  description: 'WF1: land every shared primitive (module registry, derived plan-limits, contracts/DTOs, AES-GCM crypto, brand-ramp, AJV) additively in packages/common + packages/contracts + frontend lib. Nothing wired into a request path -> v1 demo cannot regress.',
  phases: [
    { title: 'Create', detail: '5 agents create disjoint new primitive files (no index edits)' },
    { title: 'Wire+Build', detail: '1 agent writes derived plan-limits, wires exports, builds common+contracts' },
    { title: 'Verify', detail: 'tsc + backward-compat (old requisition fixture still validates) + no-fabrication' },
  ],
}

const REPORT = {
  type: "object", additionalProperties: false,
  properties: {
    files: { type: "array", items: { type: "string" } },
    summary: { type: "string" },
    buildClean: { type: "boolean" },
    exports: { type: "string", description: "exact new export names + signatures for the wire step + downstream WFs" },
    notes: { type: "string" },
  },
  required: ["files", "summary", "buildClean", "exports", "notes"],
}

const CTX = "Repo D:/CDC/ATS — multi-tenant AI ATS, TypeScript, monorepo (npm workspaces + turbo). packages/common (@cdc-ats/common) and packages/contracts (@cdc-ats/contracts) compile to dist (downstream services import dist, so they must build clean). READ neighboring files first to match idioms exactly (look at existing packages/common/src structure, the existing PLAN_LIMITS copies, packages/contracts/src/dtos/requisition.ts FormFieldSchema, existing zod usage). HARD RULES: every change ADDITIVE (new files / new optional fields) so the frozen v1 demo is byte-identical; NO mock/sample/fabricated data; no em/en dashes in authored copy. Do NOT edit packages/common/src/index.ts in the Create phase (the Wire phase owns it to avoid a race). This is WF1 of a 10-workflow enterprise build (customizable dashboard + module system + OA platform); these primitives are consumed by later WFs.";

phase('Create')
log('5 agents create the shared primitive files (disjoint)')
const created = (await parallel([
  () => agent(CTX + "\n\n=== SLICE A1: MODULE REGISTRY (canonical, in-code) ===\nCreate packages/common/src/modules/types.ts (ModuleManifest type: { key:string; name:string; version:string (semver); category:string; type:'agent'|'capability'|'feature'; requiresPlan?:'FREE'|'STARTER'|'PROFESSIONAL'|'ENTERPRISE'; defaultEnabled:boolean; dependencies:{key:string;range:string}[]; capabilities:string[]; permissions:string[]; contributions:{ nav?:{label:string;href:string;roles:string[]}[]; routes?:string[]; widgets?:string[]; gatewayRoutes?:string[]; workers?:string[]; natsSubjects?:string[]; agentTypes?:string[] }; failMode?:'open'|'closed' }) and packages/common/src/modules/registry.ts: a canonical MODULE_REGISTRY: ModuleManifest[] enumerating the app's real modules (derive the list from the existing agent set + features: e.g. core-hiring, ai-screening, ai-sourcing, jd-author, interviews, scheduling, offers, copilot, analytics, billing, compliance, oa-assessments(requiresPlan:'PROFESSIONAL',defaultEnabled:false), custom-dashboards, white-label-embed, etc. — base these on the REAL agent types in packages/ai-engine + the existing PLAN_LIMITS + cd-shell NAV; do NOT invent fictional features). Plus validateRegistry(reg): Kahn topological sort that THROWS on dependency cycles or missing deps. Export a typed getModule(key) helper. Do NOT edit src/index.ts. In `exports` give the exact export names + the manifest shape.", { label: 'a1:registry', phase: 'Create', schema: REPORT }),

  () => agent(CTX + "\n\n=== SLICE A3: CONTRACTS / DTOs ===\nIn packages/contracts: (1) EDIT src/dtos/requisition.ts FormFieldSchema to add OPTIONAL fields correctAnswer (z optional, string or string[]), points (z.number().int().optional()), timeLimit (z.number().int().optional()) — they MUST be optional so existing requisition/form payloads still validate unchanged. (2) Create src/dtos/assessment.ts (zod schemas: AssessmentSchema, QuestionSchema with type enum MCQ_SINGLE|MCQ_MULTI|TRUE_FALSE|SHORT_ANSWER|ESSAY|CODING, AssessmentInviteSchema, AttemptSchema, AnswerSchema, AssessmentResultSchema, NormalizedResultSchema — fields per the architecture spec). (3) Create src/dtos/dashboard.ts (DashboardDocumentSchema: schemaVersion, globalFilters, widgets[{instanceId,type,title,dataSourceKey,viz,config,minW,minH}], layouts{lg,md,sm,xs,xxs:[{i,x,y,w,h}]}). (4) Create src/dtos/module.ts (ModuleManifest zod + TenantModuleSchema). Append the new dtos to the contracts index (this is the contracts package index, which only you touch — safe). In `exports` list the new schema/type names.", { label: 'a3:contracts', phase: 'Create', schema: REPORT }),

  () => agent(CTX + "\n\n=== SLICE A4: AES-GCM CRYPTO ===\nCreate packages/common/src/crypto/aes-gcm.ts using node:crypto ONLY (no new npm dep): encryptConfig(obj: unknown): string and decryptConfig(payload: string): unknown — AES-256-GCM envelope (random 12-byte IV per encrypt, auth tag, base64 packed as iv.tag.ciphertext), key from env ATS_CONFIG_ENC_KEY (32-byte, hex or base64; throw a clear error if missing/wrong length). Add isEncrypted(payload) so callers can detect plaintext for backward-compatible read (encrypt-on-next-write migration). Do NOT edit src/index.ts. In `exports` give the function signatures + the env var name + the packed format.", { label: 'a4:crypto', phase: 'Create', schema: REPORT }),

  () => agent(CTX + "\n\n=== SLICE A5: BRAND RAMP (frontend) ===\nCreate apps/frontend/lib/theme/brand-ramp.ts: brandRamp(hex: string): { light: Record<string,string>; dark: Record<string,string> } generating the FULL Aurora token family from ONE brand hex — read apps/frontend/components/cd/cd-tokens.css to mirror the EXACT token names + oklch style used there: --brand, --brand-2, --brand-ink, --brand-tint, --brand-tint-2, --on-brand, plus the bare-channel form and the --c-brand alias, in BOTH light (.cd-scope) and dark (.dark .cd-scope) forms. Convert hex -> oklch properly (sRGB->linear->OKLab->OKLCH); derive the lightness/chroma ramp to match the existing emerald token relationships. Add validateContrast(fg,bg): { ratio:number; passesAA:boolean } (WCAG relative luminance) and auto-pick on-brand text color (the 0.179 luminance rule). Pure functions, no React, no side effects. In `exports` give the function signatures + the exact token keys produced.", { label: 'a5:ramp', phase: 'Create', schema: REPORT }),

  () => agent(CTX + "\n\n=== SLICE A6: AJV VALIDATION CORE ===\nCreate packages/common/src/validation/ajv.ts: a shared AJV 8 instance (allErrors, strict:false, coerceTypes off) + compileSchema(schema) + a validateOrThrow(schema, data) helper that maps AJV errors to a clean { field, message }[] shape (mirror the error envelope used by packages/common/src/middleware/error-handler.ts so OA schema/submission validation surfaces 400 VALIDATION_ERROR consistently). This is the server-side trust boundary for OA assessment schemaJson + candidate submissions. ajv@8 is a NEW dependency of packages/common — add it to packages/common/package.json dependencies. Do NOT edit src/index.ts. In `exports` give the signatures.", { label: 'a6:ajv', phase: 'Create', schema: REPORT }),
])).filter(Boolean)
log('Create done: ' + created.length + '/5')

phase('Wire+Build')
log('Wire derived plan-limits + exports, build common+contracts')
const wire = await agent(CTX + "\n\n=== SLICE A2 + WIRE + BUILD ===\nThe Create phase added: modules/registry.ts + modules/types.ts (MODULE_REGISTRY, validateRegistry, getModule), crypto/aes-gcm.ts (encryptConfig/decryptConfig/isEncrypted), validation/ajv.ts (compileSchema/validateOrThrow), and the contracts dtos. Read their actual exports first.\n1) SLICE A2: create packages/common/src/modules/plan-limits.ts that DERIVES the PLAN_LIMITS object (FREE/STARTER/PROFESSIONAL/ENTERPRISE -> { agents:string[]; customForms:boolean; configurableRounds:boolean; activeJobs:number; ... whatever the existing PLAN_LIMITS copies contain }) FROM the MODULE_REGISTRY (a module with requiresPlan X contributes its agentTypes to plans >= X). FIRST read the existing PLAN_LIMITS copies (grep PLAN_LIMITS across apps/billing-service, apps/job-service, apps/identity-service) and make the derived output BYTE-EQUAL to the current FREE/PROFESSIONAL/ENTERPRISE values (this is a pure refactor; assert equivalence in a comment). Export derivePlanLimits() + the resulting PLAN_LIMITS const. Do NOT yet edit the 3 in-service copies (that is WF3-C5).\n2) WIRE EXPORTS: edit packages/common/src/index.ts to export the new modules (modules/registry, modules/types, modules/plan-limits, crypto/aes-gcm, validation/ajv). Verify packages/contracts index exports the new dtos.\n3) BUILD: run `npm run build --workspace=@cdc-ats/common` and `npm run build --workspace=@cdc-ats/contracts`; if ajv was added, run npm install first. Report tsc result honestly (buildClean). In `exports` give the final public surface of @cdc-ats/common's new additions.", { label: 'a2:wire', phase: 'Wire+Build', schema: REPORT })
log('Wire done: buildClean=' + (wire && wire.buildClean))

phase('Verify')
const verify = (await parallel([
  () => agent("Build verification for D:/CDC/ATS. Run verbatim and report each: cd packages/common && npm run build ; cd packages/contracts && npm run build. REPORT: files=any with errors, summary=pass/fail, buildClean=all green, exports='', notes=every error verbatim.", { label: 'vf:build', phase: 'Verify', schema: REPORT }),
  () => agent(CTX + "\n\n=== AUDIT: backward-compat + no-fabrication (read-only) ===\nVerify citing file:line: (1) FormFieldSchema's new fields (correctAnswer/points/timeLimit) are OPTIONAL — construct/parse an OLD requisition form fixture (only the original fields) and confirm it still validates. (2) the derived plan-limits FREE/PROFESSIONAL/ENTERPRISE output equals the existing in-service PLAN_LIMITS values (no entitlement drift). (3) validateRegistry detects a cycle (reason about a fabricated cyclic manifest). (4) crypto throws clearly when ATS_CONFIG_ENC_KEY is missing/short. (5) NO hardcoded sample/mock data in any new file; the MODULE_REGISTRY entries correspond to REAL app features/agents (not invented). (6) packages/common/src/index.ts exports the new modules and nothing references them in a request path yet. Return REPORT: files=violations, summary=verdict, buildClean=true iff clean, exports='', notes=each issue with file:line.", { label: 'vf:audit', phase: 'Verify', schema: REPORT }),
])).filter(Boolean)

return { created, wire, verify }
