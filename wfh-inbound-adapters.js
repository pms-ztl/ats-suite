export const meta = {
  name: 'wfh-feed-inbound-adapters',
  description: 'WF-H: the remaining feed/credential adapters (Dice/Adzuna/Jooble/Wellfound/Foundit-Shine) + the inbound apply-webhook ingest route (RAW body -> verify -> parse -> Candidate/Application + resume forward + NATS, identical to public apply) + shared HMAC verify. Write-only.',
  phases: [
    { title: 'Adapters', detail: 'dice / adzuna+jooble / wellfound+foundit-shine / verify-signature (disjoint)' },
    { title: 'Ingest', detail: 'inbound-job-application route + register feed adapters in registry' },
  ],
}

const REPORT = { type: "object", additionalProperties: false, properties: {
  files: { type: "array", items: { type: "string" } }, summary: { type: "string" }, contract: { type: "string" }, notes: { type: "string" },
}, required: ["files", "summary", "contract", "notes"] };

const CTX = "Repo D:/CDC/ATS. EXECUTION RULE: write SOURCE ONLY — no npm/npx/tsc/prisma/docker (orchestrator builds; those pop a Windows dialog here). NodeNext ESM -> relative imports end in .js. READ FIRST + clone: apps/job-service/src/providers/hiringplatform/{types.ts,index.ts,feed.ts,http.ts,provider-creds.ts,ziprecruiter.ts,indeed.ts} (WF-E/F/G), the existing public apply path apps/job-service/src/routes/public.ts (apply-custom: dedupe Candidate by (tenantId,email), create Application via the internal applications path, store resume to MinIO + forward to resume-service -> parse -> screen), the gateway RAW inbound proxy already added for /api/inbound-job-application (WF-E E6), the assessment inbound-assessment route as the structural template, and how NATS events are published (publishEvent + absolute APP_URL links). HARD RULES: real-data-or-null (parseApplication returns null on a non-apply event -> no phantom candidate; NEVER synthesize an applicant); verifyWebhook over EXACT raw bytes (timing-safe); credential-stored connectors with NO public API ship behind a clear PENDING_PARTNER_APPROVAL / 'no public API' flag (NO scrapers); board applies create REAL Candidates ONLY from real signed payloads; no auto-reject; no em/en dashes. This is WF-H of a 9-workflow program.";

phase('Adapters')
log('dice / adzuna+jooble / wellfound+foundit-shine / verify-signature')
const ad = (await parallel([
  () => agent(CTX + "\n\n=== H1: Dice (NEW .../hiringplatform/dice.ts) ===\nexport const diceProvider (id 'dice', caps feed:true; postApi false). A FEED-PUBLISHER adapter: toFeedEntry(job) delegates to the WF-F feed.ts buildSourceFeed/<job> entry (Dice Job Bot scrapes the WF-F feed; hashtag-case-sensitive routing noted in a comment). postJob throws/returns PENDING_PARTNER_APPROVAL (ATS Direct requires a signed addendum + secret key; no public REST). parseApplication: apply return is apply-redirect, so null unless an ATS-Direct postback shape is configured. Flag: partner addendum required. In `contract` give caps + the feed-delegate.", { label: 'h1:dice', phase: 'Adapters', schema: REPORT }),
  () => agent(CTX + "\n\n=== H2: Adzuna + Jooble (NEW .../hiringplatform/adzuna.ts + jooble.ts) ===\nThin pull-feed adapters delegating to the WF-F feed.ts transforms: adzunaProvider (id 'adzuna', caps feed:true) -> toFeedEntry delegates to toAdzunaFeed; no per-call auth (register feed URL); partnerGated setup but no per-call secret. joobleProvider (id 'jooble', caps feed:true; NOT partner-gated) -> toFeedEntry delegates to toJoobleFeed (DD.MM.YYYY, inline currency). Both: postJob is a no-op/feed-only (return a feed-published status, NOT a fake externalId); parseApplication null (organic boards redirect to our applyUrl). In `contract` give both adapters + the feed delegation.", { label: 'h2:adzuna-jooble', phase: 'Adapters', schema: REPORT }),
  () => agent(CTX + "\n\n=== H3: Wellfound + Foundit/Shine (NEW .../hiringplatform/wellfound.ts + foundit-shine.ts) ===\nwellfoundProvider (id 'wellfound', caps feed:true): INVERTED model (Wellfound PULLS your ATS feed; no outbound publish API) -> expose via the generic feed; postJob returns PENDING_PARTNER_APPROVAL with a 'inverted/relationship integration' note; an applicant-writeback parseApplication only if a configured shape arrives. founditShineProvider(s) (id 'foundit' + id 'shine', caps {} mostly false): credential-stored connectors with NO documented public API -> postJob/parseApplication return PENDING_PARTNER_APPROVAL / 'no public API' flag; NO scrapers, NO fabricated endpoints. These are the lowest-priority honest stubs. In `contract` give the providers + the no-public-API flags.", { label: 'h3:wellfound', phase: 'Adapters', schema: REPORT }),
  () => agent(CTX + "\n\n=== H5: shared signature verify (NEW .../hiringplatform/verify-signature.ts) ===\nexport verifySignature(provider:ProviderKey, rawBody:string, headers, secret?):boolean dispatching per-provider HMAC: Indeed = HMAC-SHA1 over rawBody == X-Indeed-Signature; LinkedIn = HMAC-SHA256 == X-LI-Signature; ZipRecruiter = base64 HMAC-SHA256 over `${timestamp}.${rawBody}` == X-ZipRecruiter-Signature; Naukri = shared-secret header compare; others = delegate to requireProvider(provider).verifyWebhook. ALL comparisons timing-safe (crypto.timingSafeEqual, length-guarded). In `contract` give the dispatch table.", { label: 'h5:verify', phase: 'Adapters', schema: REPORT }),
])).filter(Boolean)
log('Adapters done: ' + ad.length + '/4')

phase('Ingest')
log('inbound route + register feed adapters')
const ing = (await parallel([
  () => agent(CTX + "\n\n=== H4: inbound apply ingest (NEW apps/job-service/src/routes/inbound-job-application.ts + register) ===\nPOST /internal/inbound-job-application/:provider/:tenantId mounted with express.raw({type:'*/*'}) so the RAW body is preserved BEFORE any JSON parse (HMAC needs exact bytes). Flow: verifySignature(provider, rawBody, headers, secret-from-creds) -> reject (provider-appropriate non-2xx) on failure; requireProvider(provider).parseApplication(rawBody) -> if null, ack-and-ignore (no phantom candidate); else dedupe Candidate by (tenantId,email) + create Application via the EXISTING internal applications path, store the resume to MinIO (download from resume.mediaUrl if present, else decode base64) + forward to the resume->parse->screen pipeline IDENTICALLY to public apply-custom, publish NATS jobboard.application.received, return the provider-specific 2xx (Indeed EXACT codes 200/409/410/400/401/404/413/422 per the indeed adapter's documented mapping; LinkedIn 200 or non-200+errorCode; others 200). Use prismaAdmin (no auth ctx; tenant from the :tenantId path + the invite/job correlation). In `contract` give the route + the ingest mapping + the per-provider response codes.", { label: 'h4:ingest', phase: 'Ingest', schema: REPORT }),
  () => agent(CTX + "\n\n=== WIRE: register feed adapters (EDIT apps/job-service/src/providers/hiringplatform/index.ts) ===\nADD to the PROVIDERS map (the WF-F+G WIRE already registered ziprecruiter/indeed/linkedin/seek/naukri/google-jobs): dice (./dice.js), adzuna (./adzuna.js), jooble (./jooble.js), wellfound (./wellfound.js), foundit + shine (./foundit-shine.js). Ensure getProvider/requireProvider/providerSupportsFeed resolve for all 12 ProviderKeys. Do NOT duplicate existing registrations. In `contract` give the final complete registered ProviderKey set + caps summary (which support postApi vs feed vs webhook).", { label: 'wire:feed', phase: 'Ingest', schema: REPORT }),
])).filter(Boolean)
log('Ingest done: ' + ing.length + '/2')

return { ad, ing }
