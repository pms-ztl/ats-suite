export const meta = {
  name: 'devops-scalability-10k',
  description: 'Make the ATS best-in-class scalable + easy to manage for 10k+ concurrent users: a values-driven Helm chart, KEDA event-driven worker autoscaling, PgBouncer connection pooling, complete k8s hardening (probes/resources/PDB/NetworkPolicy/Ingress+TLS), Prometheus/Grafana observability, CI/CD, an operations runbook, and a production Podman/compose variant. 3 disjoint lanes, write-only DEPLOY ARTIFACTS ONLY; orchestrator validates YAML.',
  phases: [
    { title: 'Build', detail: '3 lanes: helm-chart / k8s-scale (KEDA+PgBouncer+hardening) / observability-cicd-docs' },
  ],
}

const REPORT = { type: "object", additionalProperties: false, properties: {
  files: { type: "array", items: { type: "string" } },
  summary: { type: "string" },
  serviceList: { type: "string", description: "the real service list + ports + key env you grounded the manifests in" },
  notes: { type: "string" },
}, required: ["files", "summary", "serviceList", "notes"] };

const CTX = "Repo D:/CDC/ATS - a SHIPPED multi-tenant AI ATS: 15 Express microservices under apps/* (api-gateway, identity, tenant, job, candidate, resume, screening, assessment, interview, collab, onboarding, notification, billing, analytics, search) + Next.js apps/frontend, backed by Postgres (DB-per-service), Redis (BullMQ queues + rate limits), NATS (events), MinIO (S3). CLIENT: NCR Voyix; the ask is 10,000+ concurrent users, best-in-class microservices, easy to manage. EXECUTION RULE: write DEPLOY ARTIFACTS ONLY (YAML/Helm/Dockerfiles/shell/Markdown/CI) - NO npm/npx/tsc/prisma/docker/helm/kubectl commands (orchestrator validates; those pop a dialog / need a cluster not present here). GROUND EVERYTHING IN REALITY: READ docker-compose.demo.yml (the REAL service list, container names, ports, env vars, depends_on, healthchecks), infra/Dockerfile.service + infra/Dockerfile.frontend (how images build), deploy/k8s/* (the manifests already written by a prior batch: Deployments/Services/StatefulSets/migrator Job/Ingress/kustomization + 55-autoscalers.yaml HPA), and docs/SCALABILITY.md + docs/PRODUCTION_READINESS.md (the honest scaling story already documented). Use the ACTUAL service names/ports/env - do not invent services. HARD RULES: additive (do not break the working docker-compose.demo.yml or the existing raw deploy/k8s manifests unless you are the lane that owns hardening them); HONEST - these manifests are derived from the compose stack and NOT tested against a live cluster, say so in the READMEs; real values, no fabricated metrics; no em or en dashes in authored text; never hardcode real secrets (use k8s Secret refs / values placeholders / env). Stay STRICTLY in your lane's file territory to avoid collisions with the parallel lanes.";

phase('Build')
log('3 DevOps lanes: helm / k8s-scale / observability-cicd-docs')
const out = (await parallel([
  () => agent(CTX + "\n\n=== LANE A: HELM CHART (territory: NEW deploy/helm/ ONLY) ===\nDeliver the 'easy to manage' answer: a complete, values-driven Helm chart deploy/helm/ats-suite/ that deploys the WHOLE stack.\n- Chart.yaml + a rich values.yaml: per-service block (image repo/tag, replicaCount, resources requests/limits, env, service port) driven from the REAL services/ports in docker-compose.demo.yml; global blocks for postgres/redis/nats/minio (as dependencies or subchart refs or external endpoints), ingress (host/TLS/className), autoscaling (HPA min/max/targetCPU per service), and per-worker KEDA toggle.\n- templates/: a reusable _helpers.tpl, and templated Deployment + Service + HPA + PodDisruptionBudget for each app service (loop over .Values via a range where clean), each with readiness + liveness + startup probes on the REAL health endpoints, resource requests/limits, securityContext (non-root), and rolling-update strategy. A StatefulSet path (or external-endpoint values) for postgres/redis/nats/minio. The migrator as a Helm hook Job (pre-install/pre-upgrade). ConfigMap for non-secret env + a Secret template (values-injected, never a real secret). Ingress with TLS + path routing to gateway + frontend. Optional ServiceMonitor when monitoring.enabled.\n- values-dev.yaml (1 replica, low resources) + values-prod.yaml (HPA on, higher min replicas on hot-path gateway/job/resume/frontend, PDBs) demonstrating the 10k posture.\n- deploy/helm/ats-suite/README.md: install/upgrade commands (helm upgrade --install), the values knobs, the scaling story (how replicas + HPA + KEDA reach 10k), and the honest 'derived-from-compose, validate against your cluster' caveat.\nReport the real service list + ports you templated.", { label: 'do:helm', phase: 'Build', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE B: KEDA + PgBouncer + RAW-K8S HARDENING (territory: deploy/k8s/ ONLY - you MAY edit the existing deploy/k8s/*.yaml service manifests + kustomization.yaml, and ADD new files; do NOT create a deploy/k8s/observability/ dir (Lane C owns that)) ===\nMake the raw-kustomize path production-grade for 10k and add the two scaling primitives that actually matter.\n1. KEDA ScaledObjects (NEW deploy/k8s/60-keda-scaledobjects.yaml): event-driven autoscaling of the BullMQ QUEUE WORKERS by Redis list/stream length - resume-parser (parse + bulk-archive), screening worker, assessment provider-invite + poll workers, job apply-ingest + board workers, notification delivery. Use the KEDA redis/redis-streams scaler pointing at the real Redis; sensible minReplicaCount (1) / maxReplicaCount (high for hot queues) / lag thresholds. Add a short comment that KEDA CRDs must be installed. This is the RIGHT tool to scale the async pipeline that absorbs a 10k apply burst (HPA-by-CPU does not see queue lag).\n2. PgBouncer (NEW deploy/k8s/25-pgbouncer.yaml): a PgBouncer Deployment + Service (transaction pooling) in front of Postgres so thousands of app connections fan into a small server-side pool - essential at 10k. Include a ConfigMap for pgbouncer.ini/userlist (values/secret placeholders, no real creds) and note that services point their DATABASE_URL at pgbouncer; note the RLS SET-LOCAL / transaction-pooling caveat from docs.\n3. HARDEN the existing raw manifests: ensure EVERY app Deployment has readiness + liveness + startup probes (real health endpoints), resource requests/limits, and a PodDisruptionBudget; add a default-deny + allow NetworkPolicy set; complete any deployable missing from deploy/k8s (the prior batch covered ~11 of 17); confirm the Ingress has TLS. Extend HPA coverage (in the existing 55-autoscalers.yaml) to all hot-path HTTP services. Update kustomization.yaml to include your new files (you own it this lane; Lane C will NOT touch kustomization - it references its own overlay).\nReport the real service list + ports.", { label: 'do:k8s-scale', phase: 'Build', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE C: OBSERVABILITY + CI/CD + OPS RUNBOOK + PROD COMPOSE/PODMAN (territory: NEW deploy/k8s/observability/ + NEW .github/workflows/ + NEW deploy/compose/ + docs/ + a root Makefile ONLY; do NOT edit deploy/k8s root files or kustomization - keep observability self-contained with its OWN kustomization) ===\nMake it easy to operate + ship, and give a scaled single-host option.\n1. OBSERVABILITY (deploy/k8s/observability/, self-contained with its own kustomization.yaml): Prometheus (scrape configs for the services' /metrics - the services already expose Prometheus metrics via the metrics middleware), Grafana (a dashboard JSON for throughput/latency/error-rate/queue-depth/replica-count), Alertmanager rules (high error rate, queue backlog, pod restarts, DB saturation), and ServiceMonitor/PodMonitor CRs. Honest: note Prometheus-Operator CRDs required.\n2. CI/CD (.github/workflows/): a deploy pipeline that builds + pushes the service + frontend images (infra/Dockerfile.service matrix over the real services, infra/Dockerfile.frontend) to a registry on tag, then a deploy job (helm upgrade --install OR kubectl apply -k) gated on the existing build/test CI. Use placeholders for registry/cluster secrets (never real creds). Keep it honest (a template teams wire to their registry).\n3. PROD COMPOSE / PODMAN (deploy/compose/): a docker-compose.prod.yml (or podman-compose / Quadlet .container units) that runs the stack with MULTIPLE replicas of the hot-path services behind an NGINX/Traefik load balancer + PgBouncer + tuned Postgres, for teams that want a scaled SINGLE-HOST or Podman deployment without k8s. Honest caveat on single-host limits (cross-ref docs/SCALABILITY.md).\n4. OPS RUNBOOK (docs/OPERATIONS.md) + docs/DEPLOYMENT.md: how to deploy (Helm / kustomize / compose), scale (replicas, HPA, KEDA, PgBouncer pool sizing), monitor (dashboards, alerts), do zero-downtime rolling updates, back up Postgres/MinIO, and a 10k-load-test-day checklist (cross-ref load-tests/stress-10k.js + docs/SCALABILITY.md). Root Makefile with easy targets (make up / down / build / deploy-helm / deploy-k8s / load-test / logs).\nReport what you produced.", { label: 'do:observability-cicd', phase: 'Build', schema: REPORT }),
])).filter(Boolean)
log('Build done: ' + out.length + '/3 lanes')

return { lanes: out.map(o => ({ summary: o.summary, files: o.files.length, notes: o.notes })) }
