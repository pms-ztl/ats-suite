export const meta = {
  name: 'wf6-dashboard-persistence-edit-theming',
  description: 'WF6 (SOURCE-ONLY): turn the read-only grid into a real editor with per-user persistence (identity-service copy-on-write), AND fix the highest-value theming bug (tenant brand color never reaching the logged-in app). Additive on WF5; graceful fallback to seeded default so untouched tenants are byte-identical.',
  phases: [
    { title: 'BackendTheme', detail: '3 agents: identity dashboards API + gateway proxy, cd-shell theme inject, branding form/persist (disjoint)' },
    { title: 'EditUI', detail: '1 agent: edit toolbar/palette/settings + use-dashboard-layout write path' },
  ],
}

const REPORT = {
  type: "object", additionalProperties: false,
  properties: {
    files: { type: "array", items: { type: "string" } },
    summary: { type: "string" },
    buildClean: { type: "boolean" },
    contract: { type: "string" },
    notes: { type: "string" },
  },
  required: ["files", "summary", "buildClean", "contract", "notes"],
}

const CTX = "Repo D:/CDC/ATS. Docker is up but the DashboardLayout/UserUiPrefs DB tables + tenant theme columns are not yet db-pushed (migration is a deferred final pass) — so write SOURCE that COMPILES (prisma clients already regenerated in WF3 carry the types) and verify with host tsc / `npm run build --workspace=@cdc-ats/<svc>` and `cd apps/frontend && npx tsc --noEmit`; do NOT run docker. READ the real code first: identity-service routes + auth.ts + prisma.ts (prismaRls opt-in pattern) + apply-rls, the gateway app.ts proxy/forwardHeaders pattern, the WF5 dashboard engine (components/dashboard/*, lib/widgets/*, hooks/use-dashboard-layout.ts + use-modules.ts), components/cd/cd-shell.tsx + hooks/use-tenant-branding, apps/frontend/lib/theme/brand-ramp.ts (WF1: brandRamp(hex)->{light,dark} token maps + validateContrast), app/(dashboard)/settings/branding/page.tsx + tenant-service branding.ts, components/careers/branded-shell.tsx. HARD RULES: REAL data or honest empty states only; every change ADDITIVE + GRACEFUL (if an endpoint 404s the frontend falls back to the WF5 seeded default so untouched tenants are byte-identical); per-user writes are copy-on-write + RLS-scoped; no em/en dashes; multer routes must re-apply tenantContext AFTER multer. This is WF6 of a 10-workflow enterprise build.";

phase('BackendTheme')
log('3 agents: identity API + gateway proxy, theme inject, branding form/persist')
const bt = (await parallel([
  () => agent(CTX + "\n\n=== SLICE F1: identity dashboards API + gateway proxy ===\nNEW apps/identity-service/src/routes/dashboards.ts (mounted additively): GET/PUT/DELETE /internal/users/me/dashboards/:dashboardKey and admin GET/PUT /internal/users/tenant/dashboards/:dashboardKey + GET/PATCH /internal/users/me/preferences. Semantics: GET resolves the caller's document with order user-override -> tenant-default -> (404 if none, so the client uses its seeded default); migrate-on-read is the client's job but validate the doc on write. PUT /me/dashboards = COPY-ON-WRITE the user's own DashboardLayout row (scope='user', upsert on [tenantId,userId,dashboardKey]); full-replace the document. DELETE = remove the user row (revert to tenant/system default). Admin tenant routes require tenant-admin (set isDefault/scope='tenant_default'). preferences = UserUiPrefs upsert (RFC7386 merge for PATCH). Use the OPT-IN prismaRls client for these per-user routers (login/saga/super-admin/invite stay on admin). EDIT identity auth.ts (/internal/users/me) to ALSO return the user's UserUiPrefs (additive field) so the client hydrates in one round-trip. EDIT apps/api-gateway/src/app.ts to add gateway proxies (gatewayAuth + forwardHeaders): /api/me/dashboards/* and /api/me/preferences -> identity /internal/users/me/*, /api/tenant/dashboards/* -> identity /internal/users/tenant/*. Build: npm run build --workspace=@cdc-ats/identity-service + --workspace=@cdc-ats/api-gateway. In `contract` give the exact endpoints + request/response shapes for the EditUI slice.", { label: 'f1:identity', phase: 'BackendTheme', schema: REPORT }),

  () => agent(CTX + "\n\n=== SLICE F3: tenant theme injection (the high-value fix) ===\nToday the tenant brand color reaches ONLY the public career portal, NOT the logged-in app (the working inject lives in DEAD code components/layouts/dashboard-layout.tsx). FIX: EDIT components/cd/cd-shell.tsx to inject the tenant brand token ramp onto the .cd-scope wrapper div from useTenantBranding(): call brandRamp(brandHex) (apps/frontend/lib/theme/brand-ramp.ts) to generate the FULL Aurora token family in BOTH light and .dark forms, and apply them as inline CSS custom properties (or a styled <style> scoped to .cd-scope) so --brand/--brand-2/--brand-ink/--brand-tint/--brand-tint-2/--on-brand/--c-brand resolve to the tenant's color across the whole logged-in app. Respect the user's colorMode pref. If branding has no custom color, fall back to the existing emerald defaults (byte-identical to today). Do NOT break the existing cd-tokens defaults for tenants without branding. In `contract` describe how the ramp reaches .cd-scope + the fallback.", { label: 'f3:theme', phase: 'BackendTheme', schema: REPORT }),

  () => agent(CTX + "\n\n=== SLICE F4: branding form fix + tenant persist + portal white-label ===\n(1) EDIT app/(dashboard)/settings/branding/page.tsx: today it PUTs a NONEXISTENT route (/tenant/branding then /settings/branding) and silently no-ops while showing 'Saved'. FIX it to PUT the REAL gateway /api/branding route (verify the real route in the gateway/tenant-service), and extend the form to cover primary/secondary/accent color + tagline + defaultColorMode + a logo upload (multipart; the tenant-service upload route must re-apply tenantContext AFTER multer). Only show a real 'Saved' on a real 200. (2) EDIT tenant-service branding.ts to persist dashboardThemeTokens + defaultColorMode (WF3-C3 added the columns) with validation + an audit log line, additive to the existing branding PUT. (3) Mount the orphaned-but-real components/careers/branded-shell.tsx into the live (candidate-portal)/c/[slug]/* routes so the public board white-labels with the tenant brand. Build: npm run build --workspace=@cdc-ats/tenant-service + cd apps/frontend && npx tsc --noEmit. In `contract` give the real branding endpoint + the new persisted fields.", { label: 'f4:branding', phase: 'BackendTheme', schema: REPORT }),
])).filter(Boolean)
log('BackendTheme done: ' + bt.length + '/3')

phase('EditUI')
log('edit toolbar/palette/settings + write path')
const edit = await agent(CTX + "\n\n=== SLICE F2: dashboard edit mode + persistence write path ===\nF1 exposed /api/me/dashboards/:key (GET/PUT/DELETE) + /api/me/preferences. Build the editor on the WF5 read-only engine:\n1) NEW components/dashboard/DashboardToolbar.tsx: an Edit toggle + Save / Cancel / Reset-to-default buttons + (admin) 'Set as tenant default'. Shows a single LiveStatus (reuse dashboard-kit LiveStatus).\n2) NEW components/dashboard/WidgetPalette.tsx: an 'Add widget' panel driven by the WF5 registry, FILTERED by role + resolved modules (use-modules) + plan (exactly like cd-shell NAV filters); adding pushes a new instance (makeInstanceId) into the document.widgets + each breakpoint layout with {x:Infinity,y:Infinity} for RGL auto-place.\n3) NEW components/dashboard/WidgetSettings.tsx: a per-widget config form generated from the registry entry's config schema (title, data source where multiple allowed, viz choice from allowedViz, size).\n4) EDIT components/dashboard/WidgetGrid.tsx (+ WidgetGridInner) + WidgetFrame.tsx to honor isEditing: enable RGL isDraggable/isResizable, show a drag handle on the SectionCard header + a per-widget remove (X) + settings (gear); debounce onLayoutChange locally; commit the WHOLE document on Save (PUT /api/me/dashboards/:key), Cancel reverts to the loaded doc, Reset calls DELETE then reloads the default.\n5) EDIT hooks/use-dashboard-layout.ts: add the real GET (replace the WF5 TODO local-only read) with graceful 404->seeded-default fallback, plus the PUT/DELETE write path + copy-on-write semantics + migrateDashboard on read. EDIT app/(dashboard)/page.tsx to mount the DashboardToolbar above the WidgetGrid and pass isEditing through.\nReal-data-only: widgets still render honest empty states; no fabricated data. Build: cd apps/frontend && npx tsc --noEmit. In `contract` confirm the edit/save/cancel/reset cycle + the graceful fallback.", { label: 'f2:editui', phase: 'EditUI', schema: REPORT })
log('EditUI done: buildClean=' + (edit && edit.buildClean))

return { bt, edit }
