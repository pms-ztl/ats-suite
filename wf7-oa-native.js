export const meta = {
  name: 'wf7-oa-native-lifecycle',
  description: 'WF7 (SOURCE-ONLY): native OA author->invite->take->score->result loop on assessment-service, gated behind the oa-assessments module, advancing ApplicationStage.ASSESSMENT through HITL (no auto-reject). Judge0 code exec + LLM essay grading. Reuses the form-builder (builder) + apply renderer (runner).',
  phases: [
    { title: 'Service', detail: '1 agent: assessment-service boot wiring + router stubs mounted' },
    { title: 'Routes', detail: '4 agents: authoring / invite / public-take / results (disjoint route files)' },
    { title: 'WorkersGateway', detail: '3 agents: grading worker+Judge0, ai-engine essay grader, gateway proxy' },
    { title: 'Frontend', detail: '3 agents: list+builder / results UI / candidate runner' },
  ],
}

const REPORT = {
  type: "object", additionalProperties: false,
  properties: {
    files: { type: "array", items: { type: "string" } },
    summary: { type: "string" },
    buildClean: { type: "boolean" },
    contract: { type: "string" },
    notes: { type: "string" },
  },
  required: ["files", "summary", "buildClean", "contract", "notes"],
}

const CTX = "Repo D:/CDC/ATS. Docker is up but assessment_db is not yet migrated (deferred) — write SOURCE that COMPILES (prisma client regenerated in WF3 carries the OA model types) and verify with `npm run build --workspace=@cdc-ats/<svc>` / `cd apps/frontend && npx tsc --noEmit`; do NOT run docker. READ the real patterns FIRST and CLONE them: the assessment-service skeleton (apps/assessment-service: app.ts /healthz, src/lib/prisma.ts two-client prisma=RLS + prismaAdmin, prisma/schema.prisma with Assessment/AssessmentInvite/Attempt/Answer/ProctorEvent/AssessmentResult/QuestionBank/Question), a service that already has a BullMQ worker + NATS (screening-service: screening.worker.ts scoring loop ~200-229, its NATS publish/subscribe, readAuthHeaders boot), job-service /public token-by-slug pattern (prismaAdmin, tenant resolved from the row), the public apply renderer app/(candidate-portal)/jobs/[id]/apply/page.tsx (renders every field type + submits), the RequisitionBuilder.tsx FormBuilder + form-builder-live.tsx, packages/ai-engine interview-kit rubric agent + runAgent, the gateway app.ts proxy/forwardHeaders + requireModule (WF4) + the inbound-email RAW createProxyMiddleware (~787-795), @cdc-ats/contracts assessment DTOs (WF1), @cdc-ats/common validation/ajv (WF1). HARD RULES: REAL data or honest empty ONLY — Judge0 verdicts/LLM rubric are real, NEVER fabricate scores; no auto-reject (route to existing HITL, GDPR Art.22); hidden test cases NEVER returned to the candidate UI; public take endpoints use prismaAdmin + resolve tenant from token (no header ctx); re-apply tenantContext AFTER any multer; gate everything behind requireModule('oa-assessments'); no em/en dashes. This is WF7 of a 10-workflow enterprise build.";

phase('Service')
log('assessment-service boot + router stubs mounted')
const svc = await agent(CTX + "\n\n=== SLICE G1: service boot + router mounting ===\nComplete apps/assessment-service so it boots like a real service: ensure src/lib/prisma.ts exports prisma (RLS via ASSESSMENT_APP_DATABASE_URL) + prismaAdmin (ASSESSMENT_DATABASE_URL) (clone job-service); ensure readAuthHeaders + tenantContext middleware are wired in app.ts (authed routes get tenant context); set up a BullMQ Queue + Redis connection + a NATS client (clone screening-service). CREATE EMPTY router files (stubs that export an Express Router) and MOUNT them in app.ts so the Routes phase only fills them (no app.ts contention): src/routes/assessments.ts (authoring, mount /internal/assessments), src/routes/invites.ts (mount under /internal/assessments), src/routes/public-take.ts (mount /internal/public/assessment), src/routes/results.ts (mount /internal/assessments). Build: npm run build --workspace=@cdc-ats/assessment-service. In `contract` give the mounted base paths + the prisma/queue/nats exports the other slices import.", { label: 'g1:service', phase: 'Service', schema: REPORT })
log('Service done: buildClean=' + (svc && svc.buildClean))

phase('Routes')
log('4 route agents (disjoint files)')
const routes = (await parallel([
  () => agent(CTX + "\n\n=== SLICE G2: authoring routes (src/routes/assessments.ts) ===\nImplement authoring on prisma=RLS: GET /internal/assessments (list tenant assessments), POST /internal/assessments (create DRAFT), GET /internal/assessments/:id, PUT /internal/assessments/:id, GET/PUT /internal/assessments/:id/schema (the question/section tree; VALIDATE schemaJson with @cdc-ats/common validation/ajv on write; immutable versioned JSONB, bump version + publishedHash on publish). Use the @cdc-ats/contracts assessment DTOs. Build the service. In `contract` give the endpoints + shapes.", { label: 'g2:authoring', phase: 'Routes', schema: REPORT }),
  () => agent(CTX + "\n\n=== SLICE G3: invite routes (src/routes/invites.ts) ===\nImplement POST /internal/assessments/:id/invite on prisma=RLS: create AssessmentInvite with raw = crypto.randomBytes(32) (store tokenHash = SHA-256(raw), never the raw), expiresAt = now + 5d, status SENT, link to candidateId/applicationId. Publish NATS subject assessment.invited { tenantId, inviteId, assessmentId, candidateId, email, rawTokenUrl } where the URL is ABSOLUTE: `${process.env.APP_URL ?? 'http://localhost:3000'}/assessment/take/<raw>` (notification link MUST be absolute or it 400s). Also GET /internal/assessments/:id/invites (list, no raw tokens). Build. In `contract` give the invite payload + NATS subject.", { label: 'g3:invite', phase: 'Routes', schema: REPORT }),
  () => agent(CTX + "\n\n=== SLICE G4: public take routes (src/routes/public-take.ts) — prismaAdmin, token-gated ===\nALL endpoints use prismaAdmin and resolve tenant FROM the token row (no header context); re-apply tenantContext AFTER multer if any upload. POST /internal/public/assessment/start { token } -> hash-lookup the invite (not consumed/expired), realize randomization + FREEZE a snapshot (order + answer key) onto Attempt(IN_PROGRESS), flip invite STARTED + consumedAt, issue a short-lived sessionToken (hash stored), publish NATS attempt.started. PUT /internal/public/assessment/:attemptId/answer (autosave upsert on [attemptId,questionId]; validate session token). POST /internal/public/assessment/:attemptId/proctor-events (append ProctorEvent batch). POST /internal/public/assessment/:attemptId/submit (confirmed) -> score DETERMINISTIC items inline (MCQ/true-false exact-match against the frozen snapshot, weighted met/total like screening.worker ~200-229), set status GRADING, enqueue a BullMQ grading job for CODING + ESSAY items. Server is the clock authority (persist remainingSec on heartbeat). NEVER return hidden test cases or the answer key to the candidate. Build. In `contract` give the public endpoints + the session/snapshot model.", { label: 'g4:take', phase: 'Routes', schema: REPORT }),
  () => agent(CTX + "\n\n=== SLICE G5: results routes (src/routes/results.ts) — prisma=RLS ===\nGET /internal/assessments/:id/results (list AssessmentResult/Attempts for an assessment), GET /internal/assessments/attempts/:attemptId (full attempt detail: per-question answers, per-test-case code verdicts, per-criterion essay rationale, ProctorEvent timeline + riskScore), PATCH /internal/assessments/attempts/:attemptId/grade (human override: humanPoints per answer, recompute finalScore = humanScore over autoScore, set passed, clear needsReview) for the HITL review path. Build. In `contract` give the endpoints + the result/attempt detail shape.", { label: 'g5:results', phase: 'Routes', schema: REPORT }),
])).filter(Boolean)
log('Routes done: ' + routes.length + '/4')

phase('WorkersGateway')
log('grading worker + judge0 / essay-ai / gateway proxy')
const wg = (await parallel([
  () => agent(CTX + "\n\n=== SLICE G6+G7: grading worker + Judge0 client (assessment-service) ===\nNEW src/workers/grading.worker.ts (clone screening.worker BullMQ pattern): consumes the grading job, SHORT-CIRCUITS via @cdc-ats/common isModuleEnabled when oa-assessments is off for the tenant. For CODING items -> the Judge0 client; for ESSAY items -> call the ai-engine oa-essay-grader (G8) via runAgent (gate essay autoscore through the agent-plan path). Set needsReview when low-confidence or within ~5pts of pass; compute autoScore/finalScore/passed; status SCORED -> publish NATS assessment.completed { tenantId, assessmentId, attemptId, candidateId, applicationId, passed, score } (notification + candidate-service consume it to advance ApplicationStage.ASSESSMENT + route to the existing HITL review queue, NO auto-reject). NEW src/lib/judge0.ts: POST Judge0 /submissions/batch (one entry per TestCase: source_code, language_id, stdin, expected_output, cpu/wall/memory limits, callback_url = gateway /api/internal/judge0/callback) + an inbound callback handler updating per-test-case verdict (status 3=Accepted/4=Wrong/5=TLE/6=Compile), tally weighted passedCount/totalCount, hidden cases never surfaced; + a BullMQ timeout-reaper. Use env JUDGE0_URL (the isolated sidecar). Build. In `contract` give the worker job shape + Judge0 submission/callback contract + the assessment.completed payload.", { label: 'g6:worker', phase: 'WorkersGateway', schema: REPORT }),
  () => agent(CTX + "\n\n=== SLICE G8: ai-engine essay grader ===\nNEW packages/ai-engine/src/agents/oa-essay-grader.ts: a rubric grader cloning the interview-kit rubric agent shape — input { prompt, rubric criteria, candidateAnswer }, output per-criterion { criterion, score, max, rationale } + total + a confidence/needsReview flag, via the existing runAgent/LLM client (real LLM call, NEVER fabricated). Register agentType 'oa-grader' (add to the ai-engine AgentType union + any agent registry + billing ALL_AGENT_TYPES if required for plan-gating). Rebuild: npm run build --workspace=@cdc-ats/ai-engine. In `contract` give the agent input/output + the registered agentType.", { label: 'g8:essay', phase: 'WorkersGateway', schema: REPORT }),
  () => agent(CTX + "\n\n=== SLICE G9: gateway proxy (api-gateway app.ts) ===\nEDIT apps/api-gateway/src/app.ts additively: add app.use('/api/assessments', gatewayAuth(), requireModule('oa-assessments'), forwardHeaders(assessmentUrl, '/internal/assessments')) for authed authoring/invite/results; add the PUBLIC take proxy app.use('/api/public/assessment', ...) as a RAW createProxyMiddleware (token-gated, NO X-Internal-Service stamp, like the public apply + inbound-email blocks ~787-795) -> assessment /internal/public/assessment; add app.use('/api/internal/judge0/callback', RAW proxy) -> assessment Judge0 callback. Add the assessment-service URL to the gateway's service URL config (env ASSESSMENT_SERVICE_URL, default http://localhost:4014 / the compose host). Do NOT gate any EXISTING route. Build the gateway. In `contract` give the exact route -> assessment mappings + which are module-gated vs public.", { label: 'g9:gateway', phase: 'WorkersGateway', schema: REPORT }),
])).filter(Boolean)
log('WorkersGateway done: ' + wg.length + '/3')

phase('Frontend')
log('list+builder / results UI / runner')
const fe = (await parallel([
  () => agent(CTX + "\n\n=== SLICE G10: assessments list + builder (frontend) ===\nNEW app/(dashboard)/assessments/page.tsx (list the tenant's assessments via a new lib/assessment-api.ts client hitting /api/assessments; honest empty state; gated so it only shows when oa-assessments module is enabled, using use-modules). NEW app/(dashboard)/assessments/[id]/builder/page.tsx: a FORK of components/cd/RequisitionBuilder.tsx FormBuilder (reuse its palette/relabel/require/reorder/preview) extended for graded questions using the WF1 FormField additions (correctAnswer/points/timeLimit) + question types (MCQ/coding/essay); wire it via a clone of form-builder-live.tsx to GET/PUT /api/assessments/:id/schema. NEW lib/assessment-api.ts typed client. Build: cd apps/frontend && npx tsc --noEmit. In `contract` give the client functions + routes used.", { label: 'g10:builder', phase: 'Frontend', schema: REPORT }),
  () => agent(CTX + "\n\n=== SLICE G11: results UI (frontend) ===\nNEW app/(dashboard)/assessments/[id]/results/page.tsx: recruiter report reusing existing patterns — section breakdown, per-test-case code verdicts, per-criterion essay rationale, a ProctorEvent timeline + riskScore, and a HITL humanPoints grading panel (reuse the review-queue interaction pattern) wired to GET /api/assessments/:id/results, GET .../attempts/:attemptId, PATCH .../grade. Honest empty states; no fabricated scores. Build: cd apps/frontend && npx tsc --noEmit. In `contract` give the page + routes used.", { label: 'g11:results', phase: 'Frontend', schema: REPORT }),
  () => agent(CTX + "\n\n=== SLICE G12: candidate runner (frontend) ===\nNEW app/(candidate-portal)/assessment/take/[token]/page.tsx: a FORK of the apply renderer (app/(candidate-portal)/jobs/[id]/apply/page.tsx renderer that already renders every field type + submits) gated by the per-candidate token, adding: POST /api/public/assessment/start to begin, a SERVER-AUTHORITATIVE countdown (uses remainingSec from start; heartbeat), debounced autosave (PUT .../answer), a flag-for-review question-nav grid, explicit confirm-submit, Monaco editor (via next/dynamic({ssr:false}), loaded ONLY on this route) for CODING questions, and proctor-event capture (visibilitychange/blur/fullscreenchange/paste -> batch POST .../proctor-events). Use lib/assessment-api.ts public helpers. Never shows hidden test cases or the answer key. Build: cd apps/frontend && npx tsc --noEmit. In `contract` give the runner flow + the public endpoints used.", { label: 'g12:runner', phase: 'Frontend', schema: REPORT }),
])).filter(Boolean)
log('Frontend done: ' + fe.length + '/3')

return { svc, routes, wg, fe }
