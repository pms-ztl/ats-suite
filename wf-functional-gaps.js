export const meta = {
  name: 'functional-gap-closure',
  description: 'Close the functional defects the master-prompt audit surfaced: 3 FORBIDDEN-list violations (field-visibility UI-only, Jitsi links vs built-in room, interview-artifact clobber), dead comms wiring (stage.changed + status email, 3 dead comms, onboarding/offer delivery), and completeness (status-by-email, applicationCount rollup, resume projects/tags, roles, alignment card, MetricRollup). 6 disjoint service-territory lanes, write-only; orchestrator builds/regens prisma/tests.',
  phases: [
    { title: 'Fix', detail: '6 disjoint lanes: interview-room, rbac-visibility, candidate-core, notification-wiring, jobs-resume-analytics, frontend' },
  ],
}

const REPORT = { type: "object", additionalProperties: false, properties: {
  files: { type: "array", items: { type: "string" } },
  summary: { type: "string" },
  schemaChanged: { type: "boolean", description: "true if you edited any prisma/schema.prisma (orchestrator must regen client + migrate)" },
  contracts: { type: "string", description: "any cross-lane contract you produced/consumed (event subject, endpoint, helper signature, room URL)" },
  notes: { type: "string" },
}, required: ["files", "summary", "schemaChanged", "contracts", "notes"] };

const CTX = "Repo D:/CDC/ATS - a SHIPPED multi-tenant ATS (15 Express microservices apps/*, Next.js apps/frontend, packages/common + contracts + nats-client + ai-engine; DB-per-service Postgres + RLS, NATS events subject tenant.{tenantId}.<type>, BullMQ+Redis, MinIO, custom WebRTC interview room, Docker static images). EXECUTION RULE: write SOURCE ONLY - no npm/npx/tsc/prisma/docker (orchestrator builds, regenerates prisma clients, runs migrations + tests; those pop a Windows app-picker here). Services are NodeNext ESM (.js import specifiers). HARD RULES: everything ADDITIVE + backward-compatible (a frozen v1 demo image + the current v2 must be unaffected; do NOT change existing route/response shapes in a breaking way - additive fields only); REAL data or honest empty states, NEVER fabricated (no fake scores/counts/results); match surrounding idioms; no em or en dashes in authored text; never log or commit credentials/secrets. Stay STRICTLY inside your lane's file territory - other agents edit other services in parallel. READ the real code before editing; reuse existing patterns (the existing NATS publishEvent/subscribeToEvents, the RLS apply-rls STRICT_TABLES + policy pattern, the existing notification subscriber + email-template idioms, the existing interview artifacts route).\n\nCROSS-LANE CONTRACTS (honor these EXACT shapes so the parallel lanes interoperate):\n- EVENT (published by candidate-core, consumed by notification-wiring): subject `tenant.{tenantId}.application.stage.changed`, type `application.stage.changed`, payload { tenantId, applicationId, candidateId, fromStage, toStage, actorUserId (nullable), at (ISO string) }. tenantId is MANDATORY (multi-tenancy).\n- RBAC HELPER (written by rbac-visibility in packages/common, consumed by candidate-core + frontend-safe serialization): `filterVisibleFields<T>(entity: T, role: string, entityType: 'candidate'|'interview'|'offer'|'analytics'): Partial<T>` - strips fields the role may not see (salaries, interview notes, scores) per a central role->field matrix; unknown roles get the most restrictive view. Also export `canViewField(role, entityType, field): boolean` and the ROLE list.\n- STATUS ENDPOINT (backend in candidate-core, consumed by frontend): `GET /api/public/applications/status?email=<e>&jobSlug=<s>` -> { stage, updatedAt, jobTitle } or 404-honest-empty; no PII beyond the applicant's own stage; rate-limit-friendly.\n- BUILT-IN ROOM URL (produced by interview-room, rendered by frontend): `${APP_URL}/interview/room/{interviewId}?t=<joinToken>` - the tenant's OWN built-in WebRTC room, NEVER an external meeting URL.";

phase('Fix')
log('6 disjoint functional-gap lanes')
const fix = (await parallel([
  () => agent(CTX + "\n\n=== LANE 1: INTERVIEW-ROOM (territory: apps/interview-service + apps/collab-service ONLY) ===\nFix the collab-video FORBIDDEN violations + defects (master prompt 5.6, constraints 4 + 6):\n1. NO EXTERNAL MEETING TOOL: find where interview scheduling generates the meeting link (grep jitsi/meet.jit.si/the link field on Interview). REPLACE external (Jitsi) links with the tenant's BUILT-IN room URL `${process.env.APP_URL ?? 'http://localhost:3000'}/interview/room/{interviewId}?t=<joinToken>`. Mint a short signed/opaque join token (reuse any existing token util) so a candidate can join without an account. Do this for BOTH agent-scheduled and manual-scheduled interviews.\n2. CANDIDATE (GUEST) JOIN: add the server side so a guest with a valid join token can obtain access to the built-in room for that interview (a validate-join-token endpoint returning the room id + display identity). Do not require login.\n3. ARTIFACT CLOBBER (data-loss, constraint 6): the artifacts route currently OVERWRITES the saved notes/code/whiteboard when the room is re-entered. Fix persistence so re-entering LOADS the existing artifact and saves are non-destructive (merge/append or load-then-update; never blank-overwrite an existing InterviewArtifact). Preserve all prior content across sessions.\n4. PDF EXPORT: GET /artifact/export exists but has zero consumers - ensure it returns the full record (notes+code+whiteboard) as a PDF/document; keep the route stable so the frontend can call it.\n5. RLS BACKSTOP: add InterviewArtifact to interview-service apply-rls.ts STRICT_TABLES + the tenant policy (schemaChanged stays false unless you edit schema.prisma).\n6. interview-feedback event: if feedback creation should publish an event that notification consumes, publish `tenant.{tenantId}.interview.feedback.created` (payload { tenantId, interviewId, candidateId, applicationId, rating?/summary? }) so the dead notification listener has a real publisher. Report the exact subject in contracts.", { label: 'fix:interview-room', phase: 'Fix', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE 2: RBAC FIELD-VISIBILITY (territory: apps/identity-service + packages/common ONLY) ===\nFix the FORBIDDEN 'UI-only permission hiding' (constraint 8, master prompt 5.11, 7):\n1. Build the SERVER-SIDE field-visibility matrix + helper in packages/common (new module, exported from the package index): implement EXACTLY the cross-lane RBAC HELPER contract - `filterVisibleFields`, `canViewField`, and the ROLE list. The matrix maps role -> which sensitive fields are visible on candidate/interview/offer/analytics entities (e.g. salary/compensation, interviewerNotes, interviewScores, recruiterNotes, decision). Sane defaults: RECRUITER + HR_MANAGER see most; HIRING_MANAGER sees candidate + scores but not raw salary bands unless owner; INTERVIEWER sees interview + candidate basics, NOT salaries or other interviewers' notes; DEPARTMENT_HEAD + EXECUTIVE see analytics + decisions + salaries but not raw interviewer notes; unknown/least-privilege gets the most restrictive view. Pure functions, unit-testable, no DB.\n2. Add DEPARTMENT_HEAD + EXECUTIVE to the identity role enum/type + the RBAC permission matrix (additive; existing roles unchanged). If the role list lives in packages/contracts or identity, add there consistently.\n3. Add a dedicated vitest suite in packages/common (or identity) for the matrix: each role sees/does-not-see the right fields; least-privilege default. Report the helper signature + ROLE list in contracts.", { label: 'fix:rbac-visibility', phase: 'Fix', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE 3: CANDIDATE-CORE (territory: apps/candidate-service ONLY) ===\nWire the pipeline + status + server-side field filtering:\n1. EMIT stage.changed: in the stage-update path (the applications stage route + the new src/lib/stage-machine.ts already added), on every ACCEPTED transition publish the EVENT contract `tenant.{tenantId}.application.stage.changed` with { tenantId, applicationId, candidateId, fromStage, toStage, actorUserId, at }. Reuse the existing publishEvent from @cdc-ats/nats-client (as decision-events.ts already does). Idempotent, after the DB write commits.\n2. STATUS-BY-EMAIL endpoint: implement the STATUS ENDPOINT contract `GET /api/public/applications/status?email=&jobSlug=` (mount under the public/gateway path the frontend /status page will call). Return only the applicant's OWN stage + updatedAt + jobTitle for that job; honest 404/empty if no match; use prismaAdmin (public, no tenant context) but scope strictly by (email, jobSlug) and never leak other applicants or tenants.\n3. SERVER-SIDE FIELD FILTER: in the candidate/application serialization returned to authed dashboard callers, apply `filterVisibleFields(entity, role, 'candidate')` from @cdc-ats/common (the RBAC HELPER contract) using the caller's role (from the X-User-Role header / auth context) BEFORE responding, so restricted fields (salary, notes, scores) are stripped server-side for roles that may not see them. Additive + behind the helper; do not break the recruiter/admin full view. Report which routes you filtered in contracts.", { label: 'fix:candidate-core', phase: 'Fix', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE 4: NOTIFICATION-WIRING (territory: apps/notification-service ONLY) ===\nRepair the comms automation + dead listeners (master prompt 5.8):\n1. STATUS-UPDATE EMAIL (mandated automation): add a subscriber to the EVENT contract `tenant.*.application.stage.changed` that sends the candidate a status-update notification (in_app + email) via the existing email-template + delivery path. Use a tenant-customizable template (see 4). This is the mandated 'auto status-update email on stage change'.\n2. DEAD COMMS: fix the 3 listener/publisher mismatches so these comms actually fire: OA-invite (assessment invite email to the candidate), interview-feedback (subscribe the real `tenant.*.interview.feedback.created` subject Lane 1 now publishes), and manual-interview-invite. Align the subscriber subjects to the REAL published subjects (grep both sides); where the consumer expected a subject no one publishes, point it at the real one. Do NOT invent data - only wire the existing intended comm.\n3. ONBOARDING PORTAL INVITE: ensure the new-hire receives the onboarding portal link (subscribe the onboarding case-created/portal event, or add the missing send). Honest link via ${APP_URL}.\n4. TEMPLATE-CUSTOMIZABLE candidate comms: make the candidate-facing comms (offer, hire, rejection, status-update) resolve a tenant EmailTemplate first (reuse the existing EmailTemplate model/router) and fall back to a built-in default, so tenants can customize them. Report the events you now consume + the templates in contracts.", { label: 'fix:notification-wiring', phase: 'Fix', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE 5: JOBS + RESUME + ANALYTICS (territory: apps/job-service + apps/resume-service + apps/analytics-service ONLY) ===\nClose the intake/parse/metrics completeness gaps:\n1. applicationCount ROLLUP (job-service): the accept-fast apply path dropped the per-apply count UPDATE (to avoid a hot lock) but promised a rollup that does not exist -> JobPosting.applicationCount drifts. Add the rollup: a lightweight periodic (or event-driven on application.submitted/ingested) recompute of applicationCount = real COUNT(applications) per posting, tenant-scoped. Real counts only.\n2. RESUME projects + tags (resume-service): the structured extraction omits `projects` and `tags`. Extend the parser output + persisted Resume fields to include projects[] and tags[] (additive schema fields if needed -> set schemaChanged=true). Extract from the real parsed text (reuse the existing extraction approach); honest-empty when absent.\n3. QUARANTINE reason PERSISTED (resume-service): a corrupt file in a ZIP batch is skipped but the reason is not persisted per item. Persist the quarantine reason per ZIP item (on the manual-check/staging row or a quarantine field) so the batch result reports WHY each bad file failed. Batch must still never abort.\n4. analytics MetricRollup population (analytics-service): the /reporting funnel/summary reads an empty MetricRollup. Populate it - a rollup worker/subscriber that computes funnel/stage metrics from the real events/data it already receives (or document precisely why it stays empty if no source exists). Real data only. Report any schema changes.", { label: 'fix:jobs-resume-analytics', phase: 'Fix', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE 6: FRONTEND (territory: apps/frontend ONLY) ===\nSurface the fixes + finish the partial UI (real data, honest empty):\n1. /status page: wire the standalone candidate status page to the REAL STATUS ENDPOINT contract `GET /api/public/applications/status?email=&jobSlug=` (it currently points at a non-existent endpoint). Show the applicant's stage + updated time; honest not-found state.\n2. ALIGNMENT CARD: on the candidate profile verdict card, render the 2 missing mandated dimensions 'technical skills match' and 'experience relevance' alongside the existing overall %, strengths, missing skills - fed by the REAL screener data already on the view model (derive from requirementFindings/signals if a dedicated field is absent; honest-empty when unscored). Do NOT fabricate numbers.\n3. BUILT-IN ROOM: where the UI renders/join an interview, use the BUILT-IN ROOM URL contract (${APP_URL}/interview/room/{interviewId}?t=...) and add a candidate/guest join entry (a public join page that validates the token via Lane 1's endpoint). Remove any external-meeting-link rendering.\n4. INTERVIEW-RECORD PDF EXPORT: add the button/action that calls Lane 1's GET /artifact/export so recruiters can export the interview record (notes+code+whiteboard) as PDF.\n5. CANDIDATE SUMMARY export: wire the missing fields (interview scores, skills/experience/education/tags/notes/phone) into the existing candidate-summary export so it is the intelligent summary the spec wants. Real data, summarized. Report the surfaces changed.", { label: 'fix:frontend', phase: 'Fix', schema: REPORT }),
])).filter(Boolean)
log('Fix done: ' + fix.length + '/6 lanes; schemaChanged in: ' + fix.filter(f => f.schemaChanged).length + ' lanes')

return {
  lanes: fix.map(f => ({ summary: f.summary, files: f.files.length, schemaChanged: f.schemaChanged, contracts: f.contracts })),
  schemaChangedAny: fix.some(f => f.schemaChanged),
}
