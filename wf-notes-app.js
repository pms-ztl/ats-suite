export const meta = {
  name: 'built-in-notes-app',
  description: 'A fully-fledged Notes app inside the ATS as a NEW modular microservice (notes-service, own notes_db + RLS) plus a full Notes UI (rich tiptap editor, notebooks/folders, tags, search, pin, archive, trash/restore, autosave). Follows the assessment-service new-service precedent. 3 disjoint lanes; write-only; orchestrator builds/regens prisma/wires deploy.',
  phases: [
    { title: 'Build', detail: '3 lanes: notes-service backend / deploy wiring / frontend Notes app' },
  ],
}

const REPORT = { type: "object", additionalProperties: false, properties: {
  files: { type: "array", items: { type: "string" } }, summary: { type: "string" },
  contract: { type: "string", description: "the exact routes/ports/env the other lanes consume" },
  notes: { type: "string", description: "orchestrator follow-ups (prisma regen, new DB, deploy)" },
}, required: ["files", "summary", "contract", "notes"] };

const CTX = "Repo D:/CDC/ATS - a SHIPPED multi-tenant AI ATS. You are adding a NEW modular microservice `notes-service` + a full Notes app UI. FOLLOW THE assessment-service PRECEDENT exactly (it was the last new service added): READ apps/assessment-service (app.ts bootstrap, index.ts, package.json, tsconfig.json, prisma/schema.prisma + apply-rls.ts, lib/prisma.ts two-client RLS pattern, routes/* + readAuthHeaders/tenantContext usage), packages/common (createLogger, readAuthHeaders, tenantContext, rlsExtend, health router, errors envelope, the modules/registry.ts), apps/api-gateway/src/app.ts (how a service is proxied + forwardHeaders), docker-compose.demo.yml (the assessment-service block ~line 421 + its env at ~line 63), infra/postgres-init.sql + postgres-init-secure.sql (the DB list), infra/Dockerfile.service (generic, builds any service via tsc), and the frontend interview-room CollabNotes.tsx (the tiptap editor idiom to reuse) + a -live.tsx wrapper + how a dashboard route + nav is structured. EXECUTION RULE: write SOURCE ONLY - no npm/npx/tsc/prisma/docker (orchestrator builds, regenerates prisma, creates the DB, deploys). NodeNext ESM (.js import specifiers) for the service. HARD RULES: additive + backward-compatible (frozen v1 + current v2 unaffected); REAL data or honest empty-state ONLY (no seeded fake notes; a new user has zero notes and sees an honest empty state); tenant + per-user scoped (RLS on tenantId; notes are private to their ownerUserId within the tenant); match existing idioms; no em or en dashes in authored text; never commit secrets. Stay STRICTLY in your lane's territory.\n\nCROSS-LANE CONTRACT (all 3 lanes honor these EXACT shapes):\n- SERVICE: apps/notes-service, port 4016, env NOTES_SERVICE_URL=http://notes-service:4016, DB notes_db (DATABASE_URL + NOTES_APP_DATABASE_URL for the RLS app client), NATS/health like assessment-service.\n- BACKEND ROUTES (mounted /internal/notes with readAuthHeaders + tenantContext on the RLS client; per-user scoped by getUserId, tenant by getTenantId):\n  Notes:   GET /internal/notes?notebookId=&tag=&q=&archived=&trashed=&sort=(updated|created|title|pinned)  POST /internal/notes  GET /internal/notes/:id  PUT /internal/notes/:id  DELETE /internal/notes/:id (soft-trash)  POST /internal/notes/:id/restore  DELETE /internal/notes/:id/permanent\n  Notebooks: GET/POST /internal/notes/notebooks  PUT/DELETE /internal/notes/notebooks/:id\n  Tags:     GET/POST /internal/notes/tags  PUT/DELETE /internal/notes/tags/:id\n  (mount the literal /notebooks + /tags sub-routers BEFORE the /:id note route so they are not shadowed by :id)\n- GATEWAY: /api/notes/* -> notes-service /internal/notes/* (forwardHeaders stamps X-User-Id/X-Tenant-Id/X-User-Role from the verified JWT, like the other authed proxies).\n- MODELS (notes_db): Note { id, tenantId, ownerUserId, notebookId?, title, contentJson Json (tiptap doc), contentText String (plaintext, for search), pinned Bool, archived Bool, color?, trashedAt DateTime?, linkedEntityType?, linkedEntityId?, createdAt, updatedAt } ; Notebook { id, tenantId, ownerUserId, name, color?, sortOrder Int, createdAt } ; Tag { id, tenantId, ownerUserId, name, color? } ; NoteTag { noteId, tagId } (composite pk). All tenantId-scoped for RLS.";

phase('Build')
log('3 notes lanes: backend / wiring / frontend')
const out = (await parallel([
  () => agent(CTX + "\n\n=== LANE 1: notes-service BACKEND (territory: NEW apps/notes-service/ ONLY) ===\nBuild the complete service cloning the assessment-service structure: package.json (@cdc-ats/notes-service, same deps/scripts/build=tsc), tsconfig.json, src/index.ts (bootstrap + graceful shutdown), src/app.ts (health + requestId + metrics + tenantContext + the notes routers under /internal/notes with readAuthHeaders), src/lib/prisma.ts (two-client RLS: request client via NOTES_APP_DATABASE_URL + prismaAdmin), prisma/schema.prisma (the MODELS from the contract + indexes on (tenantId, ownerUserId), (tenantId, ownerUserId, notebookId), (tenantId, ownerUserId, updatedAt); Note.contentText for search), prisma/apply-rls.ts (STRICT/TENANT tables = Note, Notebook, Tag, NoteTag + FORCE + tenant policy), and the routers (src/routes/notes.ts, notebooks.ts, tags.ts) implementing EVERY route in the contract with real Prisma, per-user + per-tenant scoping, honest empty results. Notes CRUD: create (title+empty/optional content), update (partial: title/contentJson+derived contentText/notebookId/pinned/archived/color/tags), soft-trash (set trashedAt), restore (clear trashedAt), permanent delete; search = case-insensitive contains over title+contentText (contentText is the plaintext the frontend/or the update handler derives from the tiptap doc - derive it server-side from contentJson if the client does not send it). Notebooks + Tags CRUD, deleting a notebook moves its notes to no-notebook (not delete). Zod-validate bodies + the shared error envelope. Generated prisma client is committed in this repo (this repo tracks generated/) - the orchestrator will run prisma generate.\nReport the final route list + any deviation.", { label: 'notes:backend', phase: 'Build', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE 2: DEPLOY WIRING (territory: apps/api-gateway/src/app.ts + docker-compose.demo.yml + infra/postgres-init.sql + infra/postgres-init-secure.sql + packages/common/src/modules/registry.ts + deploy/k8s/ ONLY) ===\nWire the new notes-service into the platform, mirroring assessment-service EXACTLY:\n1. api-gateway/src/app.ts: add `const notesUrl = process.env['NOTES_SERVICE_URL'] ?? 'http://localhost:4016'` and an AUTHED proxy `/api/notes` -> notes-service `/internal/notes` with forwardHeaders (copy the assessment/candidate authed-proxy idiom incl. requireModule if the platform gates modules - gate on a new 'notes' module). Additive; do not disturb existing mounts.\n2. docker-compose.demo.yml: add a notes-service block (image built from infra/Dockerfile.service with SERVICE=notes-service like assessment-service, port 4016, DATABASE_URL=postgres notes_db, NOTES_APP_DATABASE_URL, NATS/redis/env as peers, depends_on postgres/migrator/nats), and add NOTES_SERVICE_URL to the gateway env block (~line 63 area).\n3. infra/postgres-init.sql + postgres-init-secure.sql: add CREATE DATABASE notes_db (+ the notes_user + GRANT in the -secure file, mirroring the per-service-user pattern). Note for orchestrator: existing volumes need a manual CREATE DATABASE notes_db (the assessment_db precedent).\n4. packages/common/src/modules/registry.ts: register a 'notes' module (type capability, a sensible plan tier, failMode open) so it can be entitled per tenant.\n5. deploy/k8s: add a notes-service Deployment+Service (mirror an existing service manifest) + include it in kustomization; add its DB to the notes_db expectations.\nReport every file touched + the env/DB the orchestrator must create.", { label: 'notes:wiring', phase: 'Build', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE 3: FRONTEND NOTES APP (territory: apps/frontend ONLY - NEW app/(dashboard)/notes/* route + a lane-local notes-api helper + the ONE nav file to add a 'Notes' link; do NOT edit shared lib/api.ts) ===\nBuild a fully-fledged Notes app UI at /notes consuming the gateway routes (/api/notes/*, /api/notes/notebooks, /api/notes/tags). Add API calls in a NEW lane-local lib/notes-api.ts (not shared lib/api.ts). Features (ALL of a real notes app):\n- Three-pane layout: LEFT sidebar (All notes, Pinned, Archived, Trash + a Notebooks list with create/rename/delete + a Tags list with create/filter); MIDDLE note list (search box -> /api/notes?q=, sort control updated/created/title, pinned-first, each item shows title + snippet + updated time + pin toggle); RIGHT editor pane.\n- RICH EDITOR reusing the tiptap idiom from components/cd/interview-room/CollabNotes.tsx (headings, bold/italic/underline/strike, bullet + numbered + task/checklist lists, code block, blockquote, link, horizontal rule, undo/redo) with a toolbar. AUTOSAVE (debounced PUT /api/notes/:id sending contentJson + title). New-note button. Move-to-notebook + assign-tags controls. Pin, archive, color, delete-to-trash, restore-from-trash, permanent-delete.\n- Honest empty states (no notes yet -> friendly empty; empty trash; no search results). Loading skeletons. Optimistic-but-truthful updates (reflect the real server result).\n- Add a 'Notes' entry to the dashboard sidebar nav (find the existing nav component, add one link + icon; do not restructure it).\nUse plain English UI copy (no em/en dashes). Real data only - a fresh account has zero notes. Report the surfaces + the nav file touched.", { label: 'notes:frontend', phase: 'Build', schema: REPORT }),
])).filter(Boolean)
log('Build done: ' + out.length + '/3 lanes')

return { lanes: out.map(o => ({ summary: o.summary, files: o.files.length, contract: o.contract, notes: o.notes })) }
