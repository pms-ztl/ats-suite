export const meta = {
  name: 'wfe-hiring-platform-foundation',
  description: 'WF-E: clone the assessment-provider structure into job-service for hiring platforms — HiringPlatformProvider interface + registry + creds, job-distribution module, JobBoardDistribution + ApplicationIdempotency Prisma models, notification cred-kinds, gateway outbound + RAW inbound proxies. The spine every board adapter plugs into.',
  phases: [
    { title: 'Spine', detail: '4 agents: provider types, job schema+rls, notification cred-kinds, gateway proxies (disjoint)' },
    { title: 'Plumbing', detail: '3 agents: registry (needs types), common module, http+creds helper' },
  ],
}

const REPORT = { type: "object", additionalProperties: false, properties: {
  files: { type: "array", items: { type: "string" } }, summary: { type: "string" }, contract: { type: "string" }, notes: { type: "string" },
}, required: ["files", "summary", "contract", "notes"] };

const CTX = "Repo D:/CDC/ATS. CRITICAL EXECUTION RULE: write SOURCE ONLY. Do NOT run npm, npx, tsc, prisma, docker, or ANY build/test/generate command (on this Windows host those trigger a blocking app-picker; the ORCHESTRATOR runs all generation + builds + migrations + verification afterward). Just write correct, type-clean code that matches existing idioms (correct imports, exact existing patterns). READ the real reference implementations FIRST and CLONE them structurally: apps/assessment-service/src/providers/types.ts (the AssessmentProvider interface + NormalizedResult + HARD RULES), index.ts (getProvider/requireProvider/isProviderKey registry), http.ts (timeouts + 429 backoff), provider-creds.ts (per-call AES-GCM decrypt-at-use via @cdc-ats/common); apps/assessment-service gateway wiring at apps/api-gateway/src/app.ts (the /api/assessments mount ~557-563 + the /api/inbound-assessment RAW createProxyMiddleware ~288-295 + forwardHeaders ~164 + ioredis ~54); apps/notification-service/src/lib/integration-config.ts (INTEGRATION_KINDS + SECRET_FIELDS + isAssessmentKind); packages/common/src/modules/registry.ts (MODULE_REGISTRY manifest shape + the oa-assessments entry); apps/job-service/prisma/schema.prisma + prisma/apply-rls.ts (STRICT_TABLES ~17, JobPosting model). HARD RULES (same as assessment providers): real-data-or-null (NEVER synthesize externalId/externalUrl/status), creds decrypted per-call never persisted/logged, verifyWebhook over RAW bytes, no auto-reject, per-adapter rate limits; every change ADDITIVE + module-gated so the frozen v1 demo is byte-identical; no em/en dashes. Keep the hiring-platform adapter axis DISTINCT from the assessment-provider axis (separate registries/modules). This is WF-E of a 9-workflow program.";

phase('Spine')
log('4 spine agents: types / schema+rls / notif kinds / gateway')
const spine = (await parallel([
  () => agent(CTX + "\n\n=== E1: provider interface (NEW apps/job-service/src/providers/hiringplatform/types.ts) ===\nHiringPlatformProvider interface (sibling of AssessmentProvider): { readonly id:ProviderKey ('indeed'|'linkedin'|'ziprecruiter'|'naukri'|'seek'|'dice'|'wellfound'|'google-jobs'|'adzuna'|'jooble'|'foundit'|'shine'); readonly caps:{postApi,feed,jsonLd,applyWebhook,dispositionSync,searchCandidates:boolean}; postJob(job:NormalizedJob,creds:PlatformCredentials):Promise<{externalId,externalUrl?,status,raw}>; closeJob(externalId,creds):Promise<void>; fetchJobStatus?(externalId,creds):Promise<{status,raw}>; toFeedEntry?(job:NormalizedJob):string; toJsonLd?(job:NormalizedJob):object; verifyWebhook(headers,rawBody:string,secret?):boolean; parseApplication(rawBody:string):NormalizedApplication|null; syncDisposition?(app,status:NormalizedStatus,creds):Promise<void>; fetchApplications?(since:Date,creds):Promise<NormalizedApplication[]>; searchCandidates?(query,creds):Promise<NormalizedCandidate[]> }. Define PlatformCredentials (reuse the assessment ProviderCredentials shape {apiKey,apiToken,clientId,clientSecret,webhookSecret,baseUrl,region,subdomain} + organizationId/contractId/linkedinVersion), NormalizedJob (id, externalRefs:Record<ProviderKey,{externalId,url?,status}>, title, descriptionHtml, location{country,city?,region?,postalCode?,remote:boolean}, employmentType:string[], salary?{min?,max?,currency,period}, requirements:string[], benefits:string[], department?, datePublished:ISO, validThrough?:ISO, applyUrl, contactEmail, contactPhone?), NormalizedApplication (provider, jobExternalId, externalApplyId, appliedAt:Date, candidate{firstName,lastName,email,phone?,location?}, resume?{fileName,contentType,base64?,mediaUrl?}, screenerAnswers:{question,answer}[], coverLetter?, raw), NormalizedStatus enum, NormalizedCandidate. Copy the HARD-RULES doc header from assessment types.ts. In `contract` give the full interface + normalized shapes.", { label: 'e1:types', phase: 'Spine', schema: REPORT }),
  () => agent(CTX + "\n\n=== E3: job-service Prisma models (EDIT schema.prisma + apply-rls.ts) ===\nEDIT apps/job-service/prisma/schema.prisma ADDITIVELY: model JobBoardDistribution { id String @id @default(uuid()); tenantId String; jobPostingId String; board String; externalPostingId String?; externalUrl String?; status DistributionStatus @default(PENDING); lastError String?; lastSyncedAt DateTime?; raw Json?; createdAt DateTime @default(now()); updatedAt DateTime @updatedAt; jobPosting JobPosting @relation(fields:[jobPostingId],references:[id]); @@unique([tenantId,jobPostingId,board]); @@index([tenantId]); @@index([board,status]) } + the back-relation field on JobPosting (jobBoardDistributions JobBoardDistribution[]); enum DistributionStatus { PENDING POSTING ACTIVE FAILED EXPIRED CLOSED PENDING_PARTNER_APPROVAL }; model ApplicationIdempotency { id BigInt @id @default(autoincrement()); tenantId String; idempotencyKey String; requestMethod String; requestPath String; responseCode Int?; responseBody Json?; recoveryPoint String @default(\"started\"); lockedAt DateTime?; createdAt DateTime @default(now()); lastRunAt DateTime?; @@unique([tenantId,idempotencyKey]); @@index([tenantId,createdAt]) }; model JobFeedToken { id String @id @default(uuid()); tenantId String @unique; token String @unique; createdAt DateTime @default(now()) }. EDIT prisma/apply-rls.ts STRICT_TABLES to add 'JobBoardDistribution','ApplicationIdempotency','JobFeedToken'. Do NOT run prisma generate (orchestrator does). In `contract` list the models + the STRICT_TABLES change.", { label: 'e3:schema', phase: 'Spine', schema: REPORT }),
  () => agent(CTX + "\n\n=== E4: notification cred-kinds (EDIT integration-config.ts) ===\nEDIT apps/notification-service/src/lib/integration-config.ts: extend INTEGRATION_KINDS with indeed|linkedin|ziprecruiter|naukri|seek|dice|wellfound|google-jobs|adzuna|jooble|foundit|shine (additive; existing slack/email/assessment kinds keep working). Add SECRET_FIELDS entries per new kind (e.g. clientSecret, apiToken, webhookSecret, apiKey as appropriate). Add isHiringPlatformKind(kind) (mirror isAssessmentKind) so AES-GCM sealing/redaction applies automatically. NO model/migration change (TenantIntegration reused verbatim). In `contract` give the new kinds + SECRET_FIELDS + the helper.", { label: 'e4:kinds', phase: 'Spine', schema: REPORT }),
  () => agent(CTX + "\n\n=== E6: gateway proxies (EDIT api-gateway/src/app.ts) ===\nEDIT apps/api-gateway/src/app.ts ADDITIVELY (clone the existing assessment patterns): (1) outbound: app.use('/api/job-distribution', gatewayAuth(), requireModule('job-distribution'), forwardHeaders(jobUrl,'/internal/job-distribution')) MIRRORING the /api/assessments mount; (2) inbound RAW webhook: clone the /api/inbound-assessment RAW createProxyMiddleware block for '/api/inbound-job-application/:provider/:tenantId' -> job-service /internal/inbound-job-application/:provider/:tenantId (publicWebhook, NO gatewayAuth, NO body parser/express.json, NO X-Internal-Service stamp — vendors call it; HMAC needs raw bytes); (3) public feed already covered by the existing /api/public proxy (no new route needed if feed mounts under /api/public). Do NOT gate any EXISTING route. requireModule('job-distribution') is the WF-D/E5 module (assume it exists). In `contract` give the exact mounts + which are public/raw.", { label: 'e6:gateway', phase: 'Spine', schema: REPORT }),
])).filter(Boolean)
log('Spine done: ' + spine.length + '/4')

phase('Plumbing')
log('registry + common module + http/creds')
const plumb = (await parallel([
  () => agent(CTX + "\n\n=== E2: provider registry (NEW apps/job-service/src/providers/hiringplatform/index.ts) ===\nMIRROR apps/assessment-service/src/providers/index.ts: a PROVIDERS Record<ProviderKey,HiringPlatformProvider> (start EMPTY / partially-populated; adapters register in WF-F/G/H), getProvider(key):HiringPlatformProvider|null, requireProvider(key):HiringPlatformProvider (throws on unknown), isProviderKey(x):boolean, providerSupportsWebhook(key):boolean, providerSupportsFeed(key):boolean. Import the interface from ./types.js (E1). NEVER fabricate a provider. In `contract` give the registry API.", { label: 'e2:registry', phase: 'Plumbing', schema: REPORT }),
  () => agent(CTX + "\n\n=== E5: job-distribution module (EDIT packages/common/src/modules/registry.ts) ===\nADD to MODULE_REGISTRY: { key:'job-distribution', name:'Job Distribution', version:'1.0.0', category:'hiring', type:'feature', requiresPlan:'PROFESSIONAL', defaultEnabled:false, failMode:'closed', dependencies:[], capabilities:['jobdist.post','jobdist.ingest'], permissions:['jobdist:write'], contributions:{ gatewayRoutes:['/api/job-distribution','/api/inbound-job-application'], workers:['board-post','board-sync'], natsSubjects:['jobboard.application.received'], nav:[{label:'Job Distribution',href:'/job-distribution',roles:['admin','recruiter']}] } }. KEEP DISTINCT from oa-assessments (separate axis). Additive; existing modules untouched. In `contract` give the new manifest entry.", { label: 'e5:module', phase: 'Plumbing', schema: REPORT }),
  () => agent(CTX + "\n\n=== E7: http + creds helpers (NEW apps/job-service/src/providers/hiringplatform/http.ts + provider-creds.ts) ===\nPort apps/assessment-service/src/providers/http.ts (fetch wrapper with AbortSignal.timeout, 429/Retry-After backoff, JSON+text helpers) and provider-creds.ts (load a tenant's board creds from the notification TenantIntegration store, AES-GCM decrypt-at-use via @cdc-ats/common, NEVER persist/log the plaintext) into apps/job-service/src/providers/hiringplatform/. Adapt the integration 'kind' lookup to the hiring-platform kinds. In `contract` give the http + creds helper signatures.", { label: 'e7:http', phase: 'Plumbing', schema: REPORT }),
])).filter(Boolean)
log('Plumbing done: ' + plumb.length + '/3')

return { spine, plumb }
