export const meta = {
  name: 'wffg-feed-and-programmatic-adapters',
  description: 'WF-F + WF-G: the zero-gating distribution layer (schema.org JSON-LD + one-feed-many-boards XML) AND the 5 programmatic hiring-platform adapters (ZipRecruiter/Indeed/LinkedIn/SEEK/Naukri) + outbound workers + the job-distribution route. Write-only; orchestrator builds.',
  phases: [
    { title: 'Build1', detail: 'feed builders + ziprecruiter + indeed + linkedin (disjoint files)' },
    { title: 'Build2', detail: 'seek + naukri + feed route + JSON-LD frontend' },
    { title: 'Build3', detail: 'board workers + distribution route + google-indexing worker' },
    { title: 'Wire', detail: 'register all adapters + feed providers into the registry' },
  ],
}

const REPORT = { type: "object", additionalProperties: false, properties: {
  files: { type: "array", items: { type: "string" } }, summary: { type: "string" }, contract: { type: "string" }, notes: { type: "string" },
}, required: ["files", "summary", "contract", "notes"] };

const CTX = "Repo D:/CDC/ATS. EXECUTION RULE: write SOURCE ONLY — do NOT run npm/npx/tsc/prisma/docker (the orchestrator builds + verifies; on this host those commands pop a blocking Windows dialog). Write correct type-clean code matching existing idioms; job-service uses NodeNext ESM so relative imports MUST end in .js. READ FIRST + clone: apps/job-service/src/providers/hiringplatform/types.ts (HiringPlatformProvider interface + NormalizedJob/NormalizedApplication/NormalizedJobStatus + PlatformCredentials — WF-E), index.ts (the PROVIDERS registry), http.ts (fetchJson timeout+429 backoff helpers), provider-creds.ts (per-call AES-GCM decrypt); the assessment vendor adapters apps/assessment-service/src/providers/*.ts as the structural template (postJob<->invite, verifyWebhook over RAW bytes, parseApplication<->parseWebhook, real-data-or-null); apps/job-service public apply path (Candidate/Application create + resume forward) for how an ingested application maps; BullMQ worker pattern (e.g. assessment-service workers + is-module-on short-circuit). HARD RULES: real-data-or-null (NEVER synthesize externalId/externalUrl/status — gated boards return status PENDING_PARTNER_APPROVAL, never a fake ACTIVE); creds decrypted per-call, never persisted/logged; verifyWebhook over EXACT raw bytes; per-adapter rate limits (respect 429); no auto-reject; ADDITIVE + behind the job-distribution module (failMode closed -> workers short-circuit when disabled); no em/en dashes. This is WF-F+WF-G of a 9-workflow program.";

phase('Build1')
log('feed builders + ziprecruiter + indeed + linkedin')
const b1 = (await parallel([
  () => agent(CTX + "\n\n=== F1: feed builders (NEW apps/job-service/src/providers/hiringplatform/feed.ts) ===\nCanonical schema.org/JobPosting builders: toJsonLd(job:NormalizedJob):object (required title/description/datePosted/hiringOrganization/jobLocation; conditional validThrough; recommended baseSalary/employmentType/identifier/directApply; TELECOMMUTE + applicantLocationRequirements for remote). PLUS a default <source>/<job> XML envelope builder buildSourceFeed(jobs:NormalizedJob[]):string (CDATA text, literal '<' escaped, ISO-8601 dates, stable referencenumber, direct applyUrl, posterEmail, validThrough) and thin per-channel transforms toAdzunaFeed (<jobs>/<job> + salary_min/max) + toJoobleFeed (<jobs>/<job id>, DD.MM.YYYY dates, inline currency). Use a small XML builder (string template or xmlbuilder2 if already a dep; if not, hand-build escaped strings — do NOT add an undeclared dep). FULL-snapshot, no deltas. In `contract` give the builder signatures + the JSON-LD shape.", { label: 'f1:feed', phase: 'Build1', schema: REPORT }),
  () => agent(CTX + "\n\n=== G1: ZipRecruiter adapter (NEW .../hiringplatform/ziprecruiter.ts) — BUILD-FIRST (simplest) ===\nexport const ziprecruiterProvider: HiringPlatformProvider (id 'ziprecruiter', caps postApi+applyWebhook+dispositionSync). postJob/closeJob/fetchJobStatus via REST CRUD https://api.ziprecruiter.com/partner/v0/job with HTTP Basic (apiKey in Authorization header on every request, via http.ts basicAuth). verifyWebhook = base64 HMAC-SHA256 over `${timestamp}.${rawBody}` compared (timing-safe) to X-ZipRecruiter-Signature (timestamp from X-ZipRecruiter-Signature-Timestamp). parseApplication maps the webhook body -> NormalizedApplication (null if not an apply event). syncDisposition optional. Real-or-null; no creds -> the worker marks PENDING_PARTNER_APPROVAL (not this file's concern). In `contract` give the adapter's methods + the webhook verify.", { label: 'g1:zip', phase: 'Build1', schema: REPORT }),
  () => agent(CTX + "\n\n=== G2: Indeed adapter (NEW .../hiringplatform/indeed.ts) — covers Indeed+Glassdoor+SO Jobs ===\nexport const indeedProvider (id 'indeed', caps postApi+applyWebhook+dispositionSync+feed). postJob via Indeed Job Sync GraphQL (POST https://apis.indeed.com/graphql, OAuth2 client_credentials token cached; mutation jobsIngest.createSourcedJobPostings; persist sourcedPostingId+employerJobId in externalRefs/raw), closeJob via expireSourcedJobsBySourcedPostingId. Respect rate tiers (150/30/1 rps by batch size; 429 backoff via http.ts). verifyWebhook = HMAC-SHA1 over RAW body == X-Indeed-Signature (timing-safe). parseApplication -> NormalizedApplication; the inbound handler (WF-H) must return EXACT Indeed codes 200/409/410/400/401/404/413/422 (document the mapping in the adapter so the route uses it). syncDisposition via partnerDisposition.send. Use graphql via plain fetch (do NOT add an undeclared dep; POST a JSON {query,variables}). In `contract` give methods + the exact-codes note + token caching.", { label: 'g2:indeed', phase: 'Build1', schema: REPORT }),
  () => agent(CTX + "\n\n=== G3: LinkedIn adapter (NEW .../hiringplatform/linkedin.ts) ===\nexport const linkedinProvider (id 'linkedin', caps postApi+applyWebhook). postJob via POST /rest/simpleJobPostings (batch; headers LinkedIn-Version (from creds.linkedinVersion, default '202603'), X-RestLi-Protocol-Version:2.0.0, x-restli-method:batch_create) then poll /rest/simpleJobPostingTasks; onsiteApplyConfiguration adds jobApplicationWebhookUrl + maps FormBuilder questions -> partnerQuestionIdentifier. OAuth2 client_credentials per-customer CHILD app (client_id/secret from creds; 30-min token cache by clientId in a module map or note Redis). verifyWebhook = HMAC-SHA256 (challenge handshake + X-LI-Signature push, timing-safe). parseApplication for EXPORT_JOB_APPLICATION (resume via 30-day signed mediaUrl -> set resume.mediaUrl so the ingest downloads to MinIO immediately; jobApplicationId verbatim). All gated -> status PENDING_PARTNER_APPROVAL when creds absent. RSC/RSC+ explicitly OUT. In `contract` give methods + token-cache + the apply postback shape.", { label: 'g3:linkedin', phase: 'Build1', schema: REPORT }),
])).filter(Boolean)
log('Build1 done: ' + b1.length + '/4')

phase('Build2')
log('seek + naukri + feed route + JSON-LD frontend')
const b2 = (await parallel([
  () => agent(CTX + "\n\n=== G4: SEEK adapter (NEW .../hiringplatform/seek.ts) ===\nexport const seekProvider (id 'seek', caps postApi+applyWebhook; AU/NZ). postJob via GraphQL postPosition(PositionOpening + PositionProfile) at the SEEK GraphQL endpoint, updatePostedPositionProfile, close; OAuth2 client_credentials (partner token); ad product via postingInstructions.seekAdvertisementProductId (from creds.contractId or job meta). Apply-with-SEEK ingest -> parseApplication. Target the 2026 Ad Sync / Enhanced Job Posting schema (NOT Classic). graphql via plain fetch JSON. In `contract` give methods + the postPosition shape.", { label: 'g4:seek', phase: 'Build2', schema: REPORT }),
  () => agent(CTX + "\n\n=== G5: Naukri adapter (NEW .../hiringplatform/naukri.ts) via Zwayam Amplify ===\nexport const naukriProvider (id 'naukri', caps postApi+applyWebhook; India). postJob/closeJob via InfoEdge/Zwayam Amplify (https://api.zwayam.com) with apiKey query param + 'Secret Key' HTTP header (shared-secret, NOT HMAC). Map to the 4 webhooks: /amplify/webhook/<ats>/jobs/{create,modify,delete} outbound; parseApplication handles the inbound /apply/stage_update. HARD GATE: requires a paid Naukri subscription + the paid integration module — set status PENDING_PARTNER_APPROVAL when creds absent; expose ONLY Amplify inbound candidate-pull (NO Resdex scrape; searchCandidates left unimplemented/flagged manual-contract-gated). verifyWebhook = shared-secret header compare (timing-safe). In `contract` give methods + the gate note.", { label: 'g5:naukri', phase: 'Build2', schema: REPORT }),
  () => agent(CTX + "\n\n=== F2: feed route (NEW apps/job-service/src/routes/feed.ts + register in job-service app) ===\nGET /internal/public/feed/:tenantSlug/jobs.xml?token=&format=indeed|linkedin|adzuna|jooble (default = the <source>/<job> envelope). Validate the per-tenant token (JobFeedToken table OR the AES-GCM TenantIntegration 'job-feed' kind). Load the tenant's REAL published JobPostings, map each to NormalizedJob, call the WF-F feed.ts builder for the requested format, return gzip'd XML (Content-Encoding gzip, Content-Type application/xml). FULL snapshot. A tenant with zero published jobs returns an empty <source> envelope (NEVER sample jobs). Materialize-to-MinIO + nightly cron is optional/noted. Mount under the existing /api/public gateway proxy (no gateway edit). In `contract` give the route + token check + formats.", { label: 'f2:feedroute', phase: 'Build2', schema: REPORT }),
  () => agent(CTX + "\n\n=== F3: Google-for-Jobs JSON-LD (EDIT apps/frontend public job page) ===\nEDIT apps/frontend/app/(candidate-portal)/jobs/[id]/page.tsx (and/or the c/[slug]/jobs/[id] variant) to embed a schema.org/JobPosting JSON-LD <script type=\"application/ld+json\"> built from the REAL job data (mirror the toJsonLd shape: required title/description/datePosted/hiringOrganization/jobLocation, conditional validThrough, recommended baseSalary/employmentType/identifier/directApply, TELECOMMUTE for remote). Render it ONLY when the job is real+published; on a closed/expired req set validThrough in the past (expiry hygiene). Frontend-only, no fabricated data. In `contract` give where the JSON-LD is emitted + the fields.", { label: 'f3:jsonld', phase: 'Build2', schema: REPORT }),
])).filter(Boolean)
log('Build2 done: ' + b2.length + '/4')

phase('Build3')
log('board workers + distribution route + google-indexing worker')
const b3 = (await parallel([
  () => agent(CTX + "\n\n=== G6: outbound board workers (NEW .../job-service/src/workers/board-post.worker.ts + board-sync.worker.ts) ===\nBullMQ workers (clone the existing job-service/assessment worker boot idiom; start them in the service index). board-post consumes {tenantId,jobPostingId,board}: load creds (provider-creds), if absent -> set the JobBoardDistribution row status PENDING_PARTNER_APPROVAL (honest, NOT a fake post); else requireProvider(board).postJob(normalizedJob, creds) with retry/backoff (respect 429 + Indeed batch tiers) -> update the row POSTING->ACTIVE/FAILED with externalPostingId/externalUrl/raw. board-sync consumes disposition-sync jobs -> syncDisposition. BOTH short-circuit via @cdc-ats/common isModuleEnabled('job-distribution') when disabled. Add enqueue helpers to a job-service queue lib. In `contract` give the job shapes + queue names + the PENDING_PARTNER_APPROVAL path.", { label: 'g6:workers', phase: 'Build3', schema: REPORT }),
  () => agent(CTX + "\n\n=== G7: distribution route (NEW apps/job-service/src/routes/job-distribution.ts + register) ===\nMounted at /internal/job-distribution (the WF-E E6 gateway proxy forwards /api/job-distribution here, module-gated). POST /:jobPostingId { boards:string[] } -> for each board upsert a JobBoardDistribution row (status PENDING) + enqueue a board-post job -> return 202 + the per-board rows. GET /:jobPostingId -> the REAL per-board {board,status,externalUrl,lastSyncedAt,lastError} rows. DELETE /:jobPostingId/:board -> enqueue closeJob + mark CLOSED. Use the RLS job-service prisma client. In `contract` give the routes + response shapes.", { label: 'g7:route', phase: 'Build3', schema: REPORT }),
  () => agent(CTX + "\n\n=== F4: Google Indexing worker (NEW .../job-service/src/workers/google-indexing.worker.ts) ===\nOptional BullMQ worker: on a job.published/job.closed NATS event, call the Google Indexing API (URL_UPDATED / URL_DELETED) for the public JobPosting page URL, authed by a service-account JWT (env GOOGLE_INDEXING_SA_JSON; if unset, the worker is a no-op / disabled — honest). JobPosting pages only, ~200/day budget. Short-circuit on a disabled job-distribution module. In `contract` give the worker behavior + the env gate.", { label: 'f4:indexing', phase: 'Build3', schema: REPORT }),
])).filter(Boolean)
log('Build3 done: ' + b3.length + '/3')

phase('Wire')
log('register all adapters + feed providers into the registry')
const wire = await agent(CTX + "\n\n=== WIRE: populate the provider registry (EDIT apps/job-service/src/providers/hiringplatform/index.ts) ===\nThe Build phases created the adapter files. EDIT index.ts to import + register into the PROVIDERS map: ziprecruiterProvider (./ziprecruiter.js), indeedProvider (./indeed.js), linkedinProvider (./linkedin.js), seekProvider (./seek.js), naukriProvider (./naukri.js). Also register a lightweight 'google-jobs' provider whose caps are jsonLd+feed only, delegating toJsonLd/toFeedEntry to the WF-F feed.ts builders (so google-jobs appears in the registry as feed-only). Ensure getProvider/requireProvider/providerSupportsWebhook/providerSupportsFeed resolve correctly for all. Do NOT add the WF-H feed adapters (adzuna/jooble/dice/wellfound/foundit/shine) yet — those register in WF-H. In `contract` give the final registered ProviderKey set + which support webhook vs feed.", { label: 'wire:registry', phase: 'Wire', schema: REPORT })
log('Wire done')

return { b1, b2, b3, wire }
