export const meta = {
  name: 'ncr-voyix-customization-and-scale',
  description: 'Customize the ATS for NCR Voyix (branded demo tenant + HackerRank/HackerEarth pre-wired, honestly inert without keys) and prepare the 10k-concurrent scalability story (10k k6 preset + SCALABILITY.md + PRODUCTION_READINESS.md + ASSESSMENT_INTEGRATION.md + k8s HPA). 3 disjoint lanes, write-only, additive; orchestrator builds/tests. The 10k RUN itself is Docker-gated (deferred to the deploy phase).',
  phases: [
    { title: 'Build', detail: '3 lanes: branding-seed, loadtest-10k, readiness-docs' },
  ],
}

const REPORT = { type: "object", additionalProperties: false, properties: {
  files: { type: "array", items: { type: "string" } },
  summary: { type: "string" },
  schemaChanged: { type: "boolean" },
  notes: { type: "string", description: "orchestrator follow-ups (prisma regen, seed run, docker) + any honesty caveat" },
}, required: ["files", "summary", "schemaChanged", "notes"] };

const CTX = "Repo D:/CDC/ATS - a SHIPPED multi-tenant AI ATS (Express microservices apps/*, Next.js apps/frontend, packages/common+contracts, DB-per-service Postgres+RLS, NATS, BullMQ+Redis, MinIO, Docker static images docker-compose.demo.yml, k8s manifests in deploy/k8s from the compliance batch). THE CLIENT IS NCR VOYIX (NYSE: VYX; retail + restaurant commerce technology; ~16k employees; Atlanta HQ). They use HackerRank AND HackerEarth for coding tests + technical assessments - and the ATS ALREADY has REAL API adapters for both (apps/assessment-service/src/providers/hackerrank.ts real HackerRank-for-Work v3 client, hackerearth.ts real Partner API + HMAC webhook) plus a credential-entry UI (apps/frontend/app/(dashboard)/settings/integrations/page.tsx) - they are inert only until the client pastes their OWN API keys. EXECUTION RULE: write SOURCE/DOCS ONLY - no npm/npx/tsc/prisma/docker (orchestrator builds/regens/seeds/deploys; those pop a Windows dialog here). NodeNext ESM (.js specifiers). HARD RULES: additive + backward-compatible (frozen v1 + current v2 unaffected); REAL data or honest empty/inert states, NEVER fabricated (an un-keyed integration shows 'needs credentials', never a fake 'connected'/fake score; docs must describe what the code ACTUALLY does today, no aspirational claims); no em or en dashes in authored text; never hardcode or commit secrets/API keys. Stay STRICTLY in your lane's file territory.";

phase('Build')
log('3 NCR Voyix lanes: branding-seed / loadtest-10k / readiness-docs')
const out = (await parallel([
  () => agent(CTX + "\n\n=== LANE 1: NCR VOYIX BRANDED TENANT + ASSESSMENT PRE-WIRING (territory: apps/seed-data + apps/tenant-service if a branding field is missing + apps/frontend branding/theme files ONLY) ===\nMake NCR Voyix a first-class branded demo tenant that showcases THEIR stack.\n1. RESEARCH NCR Voyix's real brand identity (WebFetch https://brandfetch.com/ncrvoyix.com and https://www.brandcolorcode.com/ncr-voyix) and use their REAL primary/accent colors + wordmark styling. If a color can't be confirmed, pick a close on-brand value and note it (do not fabricate a claim of exactness).\n2. READ the existing tenant branding mechanism FIRST: apps/tenant-service branding router + the Tenant model's branding/dashboardThemeTokens fields + how apps/frontend consumes tenant branding (logo, brand tokens, the CDC/landing + login theming). Then seed/configure an NCR Voyix tenant in apps/seed-data (domain e.g. ncrvoyix.demo; company name 'NCR Voyix'; brand palette; a loginable ADMIN e.g. admin@ncrvoyix.demo with a demo password documented in notes; plan ENTERPRISE so all features work). Reuse the existing seed idioms (seedStaff etc.) so it runs for new AND existing DBs and is idempotent.\n3. Pre-wire the assessment stack for this tenant (honestly INERT): enable the assessment (OA) module for NCR Voyix, and create HackerRank + HackerEarth TenantIntegration rows in a 'NEEDS_CREDENTIALS'/disconnected state (NO fake keys, NO fake 'connected') so the integrations screen shows them ready for NCR to paste keys. Seed a sample technical requisition (e.g. 'Software Engineer II') with an eligibility rule + an assessment step referencing a HackerRank test placeholder, so the demo shows the flow shape. Everything real-or-honest-inert.\n4. If the frontend needs an NCR brand token set (colors/logo) to render the tenant's theme, add it in the branding/theme territory only (do NOT touch shared unrelated components). Report the login + how to see the branding.", { label: 'ncr:branding-seed', phase: 'Build', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE 2: 10k SCALABILITY HARNESS (territory: load-tests/ ONLY) ===\nThe client wants 10,000+ simultaneous users with no crash. READ the existing load-tests/apply-custom.js (open-model ramping-arrival-rate k6 test, env MAX_VUS/RAMP_TARGET/SPIKE_TARGET, defaults 6000/300/600) + baseline.js + stress.js + README.md.\n1. Add a 10k preset: a NEW load-tests/stress-10k.js (or a documented env preset invocation) that drives the public accept-fast apply path at a 10k-simultaneous scale - preAllocatedVUs high, maxVUs 10000+, an arrival-rate ramp that models 10k concurrent applicants (e.g. ramp to ~800-1000 iters/s sustained with the async 202 pipeline absorbing the burst), discardResponseBodies, unique email per VU/iter. Keep the CI-failing thresholds (http_req_failed, p95/p99, dropped_iterations) but set them to the HONEST target for the accept-fast 202 (submit latency low because parsing is async).\n2. Add a k6 read/mixed scenario too if quick (a browse + status-poll mix) so the test isn't apply-only, but keep it optional.\n3. Update load-tests/README.md with: how to run the 10k preset inside WSL2/Linux (ulimit -n, DISABLE_RATE_LIMIT=1 + raised gateway ceilings), what 'arrival rate' vs 'concurrent VUs' means, and - CRITICALLY - the HONEST single-host caveat: one dev host running 15 services + Postgres + the k6 generator cannot sustain true 10k; the test measures the host's real ceiling, and true production 10k is reached by horizontal scaling (k8s HPA - cross-reference deploy/k8s + SCALABILITY.md from lane 3). No fabricated 'handled 10k, zero crashes' claim.\nReport the exact run command + env for the orchestrator to execute post-Docker.", { label: 'ncr:loadtest-10k', phase: 'Build', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE 3: PRODUCTION-READINESS + SCALABILITY + ASSESSMENT DOCS + HPA (territory: docs/ + deploy/k8s/ ONLY) ===\nProduce the enterprise/production-readiness story honestly, grounded in what the code ACTUALLY does.\n1. docs/SCALABILITY.md: how the ATS scales to 10k+ concurrent - the async accept-fast 202 apply path (apps/job-service public apply -> presigned MinIO upload -> BullMQ apply-ingest pipeline, submit returns fast, parsing off the request path), idempotency keys, the gateway trust-proxy + Redis rate-limit fix (WF-I I5), stateless services, DB-per-service, connection pooling. Then the HORIZONTAL scaling math: N replicas x per-replica throughput, k8s HPA on CPU/RPS, Postgres/PgBouncer + read paths. State the HONEST single-host dev caveat vs the production topology that reaches true 10k. Reference the real files.\n2. docs/PRODUCTION_READINESS.md: an honest enterprise-readiness checklist mapped to REAL code/config - multi-tenancy (RLS + tenantId on every event), auth/RBAC + server-side field visibility, health/readiness endpoints, structured logging (tenantId+requestId), graceful shutdown, idempotency, error envelope, versioned migrations, observability (Prometheus/Pino), CI, the k8s manifests + what is DONE vs a TODO (be honest: mark gaps as TODO, do not claim done what isn't). Cross-link DECISIONS.md/DEFERRED.md.\n3. docs/ASSESSMENT_INTEGRATION.md: the NCR-Voyix-facing runbook for HackerRank + HackerEarth - exactly how to connect (Settings -> Integrations -> paste HackerRank API key / HackerEarth client_id+secret+webhook secret), the invite -> take -> result-sync flow (HackerRank polling vs HackerEarth webhook callback URL to register), what data lands in the ATS (score, percentage, problems-solved sections, plagiarism), and the honest 'inert until keys added' state. Ground every step in the real code (assessment-service providers + inbound-assessment route + the integrations UI).\n4. deploy/k8s: add HorizontalPodAutoscaler manifests for the hot-path deployables (api-gateway, job-service, resume-service, screening/assessment workers, frontend) targeting CPU (+ RPS if available) min/max replicas that justify the 10k story; ensure the existing Deployments have readiness + liveness probes on their real health endpoints (add if missing). Additive; kustomization updated. Keep honest: note these are derived-from-compose + untested against a live cluster.\nReport the doc set + HPA files.", { label: 'ncr:readiness-docs', phase: 'Build', schema: REPORT }),
])).filter(Boolean)
log('Build done: ' + out.length + '/3 lanes; schemaChanged: ' + out.filter(o => o.schemaChanged).length)

return { lanes: out.map(o => ({ summary: o.summary, files: o.files.length, schemaChanged: o.schemaChanged, notes: o.notes })) }
