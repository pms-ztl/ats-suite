export const meta = {
  name: 'foundation-container-responsive-aesthetics',
  description: 'WF-Foundation: one shared .cd-page container + responsive tokens + aesthetic tuning, then strip per-page caps and fix the bad-layout pages across the dashboard',
  phases: [
    { title: 'Core', detail: '1 agent owns cd-tokens.css + globals.css + cd-shell.tsx (the shared foundation)' },
    { title: 'Pages', detail: '5 agents strip per-page caps + route screens through .cd-page on disjoint files' },
    { title: 'Verify', detail: 'frontend tsc + layout-conformance audit' },
  ],
}

const REPORT = {
  type: "object", additionalProperties: false,
  properties: {
    files: { type: "array", items: { type: "string" } },
    summary: { type: "string" },
    buildClean: { type: "boolean" },
    notes: { type: "string", description: "anything the next agents/phases must know; verbatim errors if build failed" },
  },
  required: ["files", "summary", "buildClean", "notes"],
}

const CTX = "Repo D:/CDC/ATS, Next.js 14 frontend at apps/frontend. FIRST read D:/CDC/ATS/DESIGN_SPEC.md in full — it is the single source of truth for this work (container tokens, .cd-page, breakpoints, responsive grids, aesthetics, the KEEP-zoom rule). Then read the actual files you edit and make surgical edits that match existing idioms. HARD RULES: real data or honest empty states only (NEVER fabricated/mock/0-as-empty); no em/en dashes in authored copy; the dashboard is wrapped in .cd-scope; KEEP the html{zoom:0.9} hack and calc(100dvh/0.9) (do NOT remove them). Build your slice with `cd apps/frontend && npx tsc --noEmit` before finishing and report buildClean honestly.";

phase('Core')
log('Core: shared container + tokens + shell (must land before Pages)')
const core = await agent(CTX + "\n\n=== SLICE: THE SHARED FOUNDATION (cd-tokens.css + globals.css + cd-shell.tsx) ===\nImplement DESIGN_SPEC sections 1, 2, 3, 4 (tokens + container + responsive + aesthetic tuning):\n1) cd-tokens.css (.cd-scope): ADD the page-width tokens (--page-gutter clamp(16px,1rem+1.5vw,40px), --page-max 1440px, --page-max-wide 1760px, --page-max-prose 66ch); ADD the shared `.cd-scope .cd-page` class + its [data-width=wide|prose] variants exactly per spec 1b; ADD the spacing ramp (--space-1..-12) per spec 3; ADD the status tokens (--st-good #22C55E, --st-warn #F59E0B, --st-bad #EF4444, --st-info #3B82F6, --st-nodata #9CA3AF + tinted -bg variants) per spec 7a; ADD an Okabe-Ito categorical viz token set (--viz-1..-7 = #E69F00 #56B4E9 #009E73 #F0E442 #0072B2 #D55E00 #CC79A7) per spec 5. Add responsive grid helper classes usable by screens: `.cd-grid-kpi { display:grid; grid-template-columns:repeat(auto-fit,minmax(220px,1fr)); gap:clamp(8px,2vw,24px) }` and `.cd-grid-cards { ... minmax(300px,1fr) ... }`. Define these for BOTH light .cd-scope and (where color-valued) .dark .cd-scope.\n2) globals.css: align the duplicate shell tokens at :root (--topbar:56px --rail:64px) to match the cd-scope values (--topbar:60px --rail:76px, --side stays 268px) so they cannot diverge; KEEP html{zoom:0.9}. AURORA DIAL-DOWN on dense data routes (spec 4): investigate where `.aurora` mounts and how --aurora-opacity resolves, then make dense routes (home '/', '/screening', '/analytics') render the aurora much fainter (target --aurora-opacity ~.18 light / ~.22 dark on those routes) WITHOUT killing it on marketing/auth/empty screens — implement cleanly (e.g. a data-dense flag on the .cd-scope wrapper set from pathname in cd-shell.tsx + a CSS rule). Keep brand-tint OFF of page/card backgrounds.\n3) cd-shell.tsx: (a) replace the flow branch inner `<div style={{ padding: 24 }}>{children}</div>` with `<div className=\"cd-page\">{children}</div>` (keep the outer overflowY:auto wrapper; KEEP height calc(100dvh/0.9)); (b) correct the FULL_BLEED membership per spec 1d — ADD '/chat'; REMOVE '/scheduling' and '/mobility'; keep the byte-exact board/table screens. (c) If you add a data-dense pathname flag for aurora, set it on the existing .cd-scope wrapper div. Report in notes the exact final FULL_BLEED set and the .cd-page hookup so the Pages agents can rely on it.", { label: 'core:foundation', phase: 'Core', schema: REPORT })
log('Core done: buildClean=' + (core && core.buildClean))

phase('Pages')
log('5 page agents strip caps + route through .cd-page on disjoint files')
const PG = CTX + "\n\nThe Core phase has added the shared `.cd-page` class (max-inline-size var(--page-max)=1440, margin-inline:auto, padding-inline var(--page-gutter), padding-block clamp) and the responsive grid helpers, and routes FLOW pages through .cd-page automatically via cd-shell. YOUR JOB (spec 1e + 2): on each screen below, DELETE the bespoke maxWidth + margin:0 auto + mx-auto max-w-[...] + ad-hoc outer horizontal padding so spacing comes ONLY from the shared container. For FLOW pages, just remove the cap (cd-shell's .cd-page governs). For FULL-BLEED screens (which own their own scroll), wrap the screen's inner scrollable content in `<div className=\"cd-page\">` (or apply the same rules) so they ALSO share the uniform 1440 cap + fluid gutter + centering instead of their old narrow/bespoke cap. Convert hardcoded fixed-fraction grids (repeat(4,1fr), 1.5fr 1fr, 1.7fr 1fr, fixed two-col) to the responsive auto-fit helpers / minmax per spec 2 so they reflow (1 col under ~1024px). Use clamp() gaps. Do NOT fabricate data and do NOT change data wiring — layout only.";
const pages = []
log('Wave A: home / hiring+gov screens / settings+workspace')
pages.push(...(await parallel([
  () => agent(PG + "\n\n=== FILES: HOME (layout only; data wiring is a later workflow) ===\napps/frontend/components/cd/screens/OrgOverview.tsx and apps/frontend/app/(dashboard)/page.tsx (the recruiter/HM/interviewer role homes). Remove maxWidth:1280 / max-w-[1200px] / max-w-[980px] caps; route content through .cd-page with data-width=\"wide\" (the ops home is the bento, 1760 cap). Convert OrgOverview grids (repeat(4,1fr) at :120, 1.5fr 1fr at :126/:147) and page.tsx grids (1.7fr 1fr at :208 etc.) to responsive auto-fit (KPI row -> cd-grid-kpi; chart rows -> cd-grid-cards) so they reflow. Keep all existing data fetchers + honest empty states intact.", { label: 'pg:home', phase: 'Pages', schema: REPORT }),
  () => agent(PG + "\n\n=== FILES: HIRING + GOVERNANCE SCREENS ===\nStrip caps and route through .cd-page (these are mostly full-bleed byte-exact screens that self-cap narrow): apps/frontend/components/cd/screens/Requisitions.tsx (1240), Offers.tsx (1080), Interviews.tsx (~1100), Decisions.tsx (720), InterviewerHome.tsx (980), and apps/frontend/components/cd/AnalyticsScreen.tsx (1240). Replace each bespoke maxWidth+margin:0 auto with the shared .cd-page treatment (1440). Make any fixed internal grids responsive. If a screen is a genuinely full-width data table that should fill the viewport, you may use data-width=\"wide\". Preserve all data + empty states.", { label: 'pg:hiring', phase: 'Pages', schema: REPORT }),
  () => agent(PG + "\n\n=== FILES: SETTINGS CLUSTER + WORKSPACE (fix the double-nav + triple caps) ===\napps/frontend/components/cd/screens/SettingsScreen.tsx, apps/frontend/app/(dashboard)/settings/layout.tsx (1280 + 230px nav), settings/page.tsx (1200 + 232px nav + 820 panel), settings/branding/page.tsx (820), settings/sms/page.tsx (820), and app/(dashboard)/workspace/page.tsx (1200 + CSS wrap 1100). Per spec: collapse the SETTINGS DOUBLE-NAV so only ONE settings nav renders (keep the layout.tsx nav, drop the duplicate nav+cap inside settings/page.tsx). Remove all the nested caps so content flows in .cd-page; cap only true running-text/prose blocks with data-width=\"prose\". Preserve all functionality.", { label: 'pg:settings', phase: 'Pages', schema: REPORT }),
])).filter(Boolean))
log('Wave B: bad pages (scheduling/hitl/mobility) / chat + portal')
pages.push(...(await parallel([
  () => agent(PG + "\n\n=== FILES: BAD-LAYOUT PAGES — scheduling / hitl / mobility ===\n(1) apps/frontend/components/cd/scheduling-live.tsx: Core REMOVED /scheduling from FULL_BLEED so it is now a FLOW page — DELETE its mx-auto max-w-[1100px] self-cap (it gets .cd-page now); make the body grid (auto-fit minmax(340px,1fr)) use align-items:stretch (not start) so the short left form matches the tall right column; ensure fixed-viewBox charts sit in cells that can grow. (2) apps/frontend/components/cd/screens/HITL.tsx: it stays full-bleed — DROP the maxWidth:760 self-cap (wrap inner content in .cd-page instead) and make the 380px+1fr grid responsive (collapse under ~1024px, stretch). (3) apps/frontend/components/cd/AiSurfaceScreens.tsx (mobility view): Core REMOVED /mobility from FULL_BLEED so it is FLOW now — drop the 980px center cap; let .cd-page govern. Layout only, preserve data.", { label: 'pg:badpages', phase: 'Pages', schema: REPORT }),
  () => agent(PG + "\n\n=== FILES: TEAM CHAT (fill the empty panel) + candidate-portal apply ===\n(1) apps/frontend/app/(dashboard)/chat/page.tsx: Core ADDED /chat to FULL_BLEED so it now mounts in the definite-height flex parent. Fix the chat root so the two-pane layout FILLS that height: change the root `height:100%` to `flex:1; minHeight:0` (and ensure display:flex with the panes using minHeight:0 so the thread panel fills and scrolls instead of collapsing). Keep the real thread-loading + SSE behavior intact (do not touch the data path). (2) candidate-portal apply pages (app/(candidate-portal)/jobs/[id]/apply or c/[slug]/jobs/apply): remove the double width-clamp (maxWidth:720 inside layout max-w-5xl) so the form uses a single consistent width; and if any apply page ships a FABRICATED default job on fetch failure, replace it with an honest error/empty state (real-data-only rule). Layout/honesty only.", { label: 'pg:chat', phase: 'Pages', schema: REPORT }),
])).filter(Boolean))
log('Pages done: ' + pages.length + '/5')

phase('Verify')
const verify = (await parallel([
  () => agent("Build verification for D:/CDC/ATS frontend. Run verbatim: cd apps/frontend && npx tsc --noEmit. Report REPORT: files=any files with type errors, summary=pass/fail, buildClean=true iff zero errors, notes=every error verbatim (file:line: message).", { label: 'vf:tsc', phase: 'Verify', schema: REPORT }),
  () => agent(CTX + "\n\n=== AUDIT: layout conformance (read-only) ===\nVerify the foundation landed, citing file:line: (1) cd-tokens.css defines --page-gutter/--page-max/--page-max-wide/--page-max-prose and the `.cd-page` class + status/viz tokens + grid helpers. (2) cd-shell.tsx flow branch renders children inside .cd-page (not a bare padding:24 div), and FULL_BLEED now INCLUDES /chat and EXCLUDES /scheduling + /mobility. (3) grep the edited screens (OrgOverview, page.tsx, Requisitions, Offers, Interviews, Decisions, InterviewerHome, AnalyticsScreen, SettingsScreen, settings/*, workspace, scheduling-live, HITL, AiSurfaceScreens, chat) for LEFTOVER hardcoded `maxWidth:` numeric caps / `max-w-[NNNpx]` / `margin: 0 auto` that should have been removed — list any that remain. (4) confirm settings renders only ONE nav. (5) confirm no new mock/sample/fabricated data was introduced and html{zoom:0.9} was NOT removed. Return REPORT: files=violations, summary=verdict, buildClean=true iff conformant, notes=each remaining issue with file:line.", { label: 'vf:conformance', phase: 'Verify', schema: REPORT }),
])).filter(Boolean)

return { core, pages, verify }
