export const meta = {
  name: 'wfcd-devui-completion',
  description: 'WF-C + WF-D: complete the developer-UI program — config provider + resolution + /api/me/ui-config (tenant + gateway), theme wiring (cd-shell + embed-shell via build-theme-css), the ui-customization module, the admin editor, slot mounts into real CD screens, and docs. Additive + fail-soft (untouched tenant renders byte-identical). Write-only.',
  phases: [
    { title: 'Foundation', detail: 'config provider + tenant route + ui-customization module (disjoint)' },
    { title: 'Wiring', detail: 'cd-shell + embed-shell + layout mount + gateway(C4+D4) + client mirror' },
    { title: 'UX', detail: 'admin editor + slot mounts + docs' },
  ],
}

const REPORT = { type: "object", additionalProperties: false, properties: {
  files: { type: "array", items: { type: "string" } }, summary: { type: "string" }, contract: { type: "string" }, notes: { type: "string" },
}, required: ["files", "summary", "contract", "notes"] };

const CTX = "Repo D:/CDC/ATS. EXECUTION RULE: write SOURCE ONLY — no npm/npx/tsc/prisma/docker (orchestrator builds; those pop a Windows dialog here). NodeNext ESM for services (.js imports). READ FIRST + reuse: packages/contracts/src/ui-config.ts (UiConfigSchema/UiConfig/migrateUiConfig — WF-A), apps/frontend/lib/theme/build-theme-css.ts (WF-B B5 theme sink), apps/frontend/lib/registry/{surface-registry.ts,slots.tsx,index.ts} (WF-B), apps/frontend/hooks/use-modules.ts + use-tenant-branding.ts (fail-soft cached fallback pattern to mirror), apps/frontend/components/cd/cd-shell.tsx (NAV/SPECIAL/ROLES/DENSE/FULL_BLEED + the existing buildBrandStyle + useTenantBranding inject), apps/frontend/app/(embed)/embed/embed-shell.tsx (the duplicated buildBrandStyle), apps/tenant-service (prismaRls opt-in + the branding self-service router + Tenant.dashboardThemeTokens/defaultColorMode/defaultDashboardByRole columns), apps/api-gateway/src/app.ts (the /api/me/modules mount + requireModule + forwardHeaders), packages/common/src/modules/registry.ts (MODULE_REGISTRY + the oa-assessments/job-distribution entries). HARD RULES: every change ADDITIVE + FAIL-SOFT — when /api/me/ui-config 404s or a tenant has no override, the provider returns an ALL-ENABLED/default fallback so cd-shell renders BYTE-IDENTICAL to today (do NOT regress the existing nav/theme/login); hex/font values are validated by the schema before reaching any inline <style>; NO mock/fabricated data; no em/en dashes. Do NOT mount blocking middleware at a prefix that catches pre-auth routes (the documented readAuthHeaders mount-order trap — gate per-route). This is WF-C+WF-D of a 9-workflow program (developer-customizable UI).";

phase('Foundation')
log('config provider + tenant route + module')
const found = (await parallel([
  () => agent(CTX + "\n\n=== C1: UiConfig provider (NEW apps/frontend/lib/config/ui-config-provider.tsx) ===\nUiConfigProvider + useUiConfig() modeled on hooks/use-modules.ts + use-tenant-branding.ts: client provider that fetches GET /api/me/ui-config once, caches ~5min, and FAILS SOFT to a default UiConfig (all routes enabled, no theme override, empty nav overrides) on 404/error so the app renders byte-identical for untouched tenants. Runs migrateUiConfig() on the fetched doc. Resolution precedence (documented): platform default <- env <- tenant override (from the response) <- per-user prefs. Export useUiConfig() returning the resolved UiConfig + helpers (e.g. isRouteEnabled(href), navOverride(id), copy(key,fallback)). Replace the WF-B forward-declared useUiConfig stub in lib/registry/index.ts barrel re-export if needed. In `contract` give the provider/hook API + the default fallback shape.", { label: 'c1:provider', phase: 'Foundation', schema: REPORT }),
  () => agent(CTX + "\n\n=== C3: tenant ui-config route (NEW apps/tenant-service/src/routes/ui-config.ts + register) ===\nGET /internal/.../ui-config: read the caller tenant's UiConfig from Tenant.dashboardThemeTokens (Json) via the prismaRls opt-in client, run migrateUiConfig() on read, return { success, data: UiConfig } (or a default doc if the column is empty). PUT /internal/.../ui-config (tenant-admin): validate with UiConfigSchema + migrate + persist to dashboardThemeTokens (+ defaultColorMode/defaultDashboardByRole where applicable) + an audit log line. Reuse the tenant prismaRls export + the branding router's tenant-admin guard. NO migration (columns already exist). Register the router in the tenant-service app. In `contract` give the internal route paths + request/response shapes.", { label: 'c3:tenant', phase: 'Foundation', schema: REPORT }),
  () => agent(CTX + "\n\n=== D1: ui-customization module (EDIT packages/common/src/modules/registry.ts) ===\nADD to MODULE_REGISTRY: { key:'ui-customization', name:'UI Customization', version:'1.0.0', category:'platform', type:'capability', requiresPlan:'ENTERPRISE', defaultEnabled:false, failMode:'open', dependencies:[], capabilities:['ui.theme','ui.layout','ui.slots'], permissions:['ui:customize'], contributions:{ gatewayRoutes:['/api/me/ui-config'] } }. Additive; do NOT touch existing entries (job-distribution/oa-assessments stay distinct). In `contract` give the new manifest entry.", { label: 'd1:module', phase: 'Foundation', schema: REPORT }),
])).filter(Boolean)
log('Foundation done: ' + found.length + '/3')

phase('Wiring')
log('cd-shell + embed-shell + layout + gateway + client mirror')
const wiring = (await parallel([
  () => agent(CTX + "\n\n=== C5: cd-shell data-drive + theme sink (EDIT apps/frontend/components/cd/cd-shell.tsx) ===\nData-drive the shell from useUiConfig (C1): apply nav.order/hidden/overrides to the NAV (filter+reorder+relabel, AFTER the existing role + module filters so tenant modules still win), honor routes[].enabled, density, and copy overrides; REPLACE the inline buildBrandStyle call with buildThemeCss (build-theme-css.ts, WF-B B5) fed from the resolved theme (brand/ai-accent/secondary/colorMode/density/radius/font/fluid). CRITICAL: keep the EXISTING fallback so a tenant with no UiConfig renders byte-identical (no nav/theme/login regression). In `contract` confirm the fallback + what config now drives the shell.", { label: 'c5:shell', phase: 'Wiring', schema: REPORT }),
  () => agent(CTX + "\n\n=== C6: embed-shell dedupe (EDIT apps/frontend/app/(embed)/embed/embed-shell.tsx) ===\nReplace the duplicated inline buildBrandStyle (the verbatim copy) with a call to the shared buildThemeCss (build-theme-css.ts, WF-B B5), preserving the current embed theming behavior exactly. Disjoint file from C5. In `contract` confirm the dedupe + unchanged behavior.", { label: 'c6:embed', phase: 'Wiring', schema: REPORT }),
  () => agent(CTX + "\n\n=== C2: provider mount (EDIT apps/frontend/app/layout.tsx) ===\nMount <UiConfigProvider> (C1) additively, wrapping the existing providers (above/around the current provider tree) so useUiConfig is available app-wide. Single additive wrap; do not reorder existing providers in a breaking way. In `contract` confirm the mount point.", { label: 'c2:layout', phase: 'Wiring', schema: REPORT }),
  () => agent(CTX + "\n\n=== C4+D4: gateway ui-config route (EDIT apps/api-gateway/src/app.ts) ===\nADD (additive, mirroring the /api/me/modules mount): app.use('/api/me/ui-config', gatewayAuth(), forwardHeaders(tenantUrl,'/internal/.../ui-config')) for GET (open read) and wrap the PUT branch with requireModule('ui-customization') (GET stays open per the modules read-pass convention; PUT requires the ENTERPRISE module). Do NOT gate any EXISTING route; do NOT add prefix middleware that catches other routes. In `contract` give the exact mount + which verb is module-gated.", { label: 'c4d4:gateway', phase: 'Wiring', schema: REPORT }),
  () => agent(CTX + "\n\n=== D2: client module mirror (EDIT apps/frontend/lib/modules/registry.ts) ===\nADD the ui-customization display entry (name/category/description/requiresPlan ENTERPRISE) to the CLIENT module mirror, byte-aligned with the D1 manifest, so the features page + nav filtering show it. Disjoint file. In `contract` give the added entry.", { label: 'd2:mirror', phase: 'Wiring', schema: REPORT }),
])).filter(Boolean)
log('Wiring done: ' + wiring.length + '/5')

phase('UX')
log('admin editor + slot mounts + docs')
const ux = (await parallel([
  () => agent(CTX + "\n\n=== D3: admin editor (EDIT/NEW under app/(dashboard)/settings/branding) ===\nAdd a UI-customization admin editor (a new component + a section/tab in settings/branding): auto-render forms for the UiConfig theme (color pickers for brand/ai-accent/secondary, enums for colorMode/density/radius, logo/font URL inputs), nav order/hidden/label overrides, route enable toggles, and copy overrides; writes via PUT /api/me/ui-config (module-gated; show the ENTERPRISE-gated state honestly when ui-customization is off). Reuse the InputSpec auto-form idea from the widget settings. Honest loading/empty/error; real 'Saved' only on a real 200. In `contract` give the editor location + the fields + the PUT wiring.", { label: 'd3:editor', phase: 'UX', schema: REPORT }),
  () => agent(CTX + "\n\n=== D6: slot mounts (EDIT components/cd/screens/* + cd-shell) ===\nMount the WF-B <Slot/> component at the closed-union slot positions in the REAL screens so developers/tenants can inject components there: shell.header.right + shell.nav.footer (cd-shell.tsx), candidate.detail.sidebar + candidate.detail.before (the candidate detail screen), requisition.detail.actions (requisition detail), screening.verdict.footer (screening screen), dashboard.toolbar (the dashboard toolbar). Each <Slot id ctx={{...realEntity}}/> passes the real entity as typed context. Additive — an empty slot renders nothing, so untouched screens are byte-identical. In `contract` list each slot id + the screen file + the ctx passed.", { label: 'd6:slots', phase: 'UX', schema: REPORT }),
  () => agent(CTX + "\n\n=== D5: docs (NEW docs/UI_CUSTOMIZATION.md) ===\nWrite the developer docs: the typed UiConfig contract (link the zod shape), the closed slot-id list + how to register a component into a slot (registerSurfaceComponent + the SlotBinding), the InputSpec field-type reference for widget/config auto-forms, the (ext)/[...slug] page-codegen route for adding a custom page, override-by-id precedence, the THREE editing surfaces (DTCG token files in git / env white-label defaults / the /settings/branding admin editor), and the TWO trust tiers (in-process typed React for first-party/trusted now; the deferred Saleor-style manifest+sandboxed-iframe for a future third-party marketplace). Doc-only. In `contract` give the doc outline.", { label: 'd5:docs', phase: 'UX', schema: REPORT }),
])).filter(Boolean)
log('UX done: ' + ux.length + '/3')

return { found, wiring, ux }
