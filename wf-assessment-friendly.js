export const meta = {
  name: 'assessment-friendly-hackerrank-hackerearth',
  description: 'Make the HackerRank/HackerEarth experience genuinely first-class for NCR Voyix: browse the connected vendor test library + validate the key (new backend route), one-click "Send coding test" from the candidate at the Assessment stage, prominent real-time results (score/problems-solved/plagiarism), and a guided Connect + Test-connection flow. 2 disjoint lanes (backend+gateway / frontend), write-only, additive; orchestrator builds/tests.',
  phases: [
    { title: 'Build', detail: '2 lanes: assessment-backend (list-tests + validate + proxy) / frontend UX' },
  ],
}

const REPORT = { type: "object", additionalProperties: false, properties: {
  files: { type: "array", items: { type: "string" } },
  summary: { type: "string" },
  contract: { type: "string", description: "the exact new endpoints + shapes the other lane consumes" },
  notes: { type: "string" },
}, required: ["files", "summary", "contract", "notes"] };

const CTX = "Repo D:/CDC/ATS - a SHIPPED multi-tenant AI ATS. CLIENT: NCR Voyix, who use HackerRank AND HackerEarth for coding tests + technical assessments. The assessment-service ALREADY has REAL vendor API adapters (apps/assessment-service/src/providers/*: hackerrank.ts, hackerearth.ts, codility.ts, imocha.ts, testgorilla.ts) each implementing listTests/invite/fetchResult/verifyWebhook/parseWebhook (providers/types.ts), a real invite flow (POST /internal/assessments/:id/invite with { provider, providerTestId }), result ingestion (inbound-assessment.ts webhook + assessment-poll.worker + results.ts GET /:id/results), and a credential-entry UI (apps/frontend/app/(dashboard)/settings/integrations/page.tsx, creds AES-GCM encrypted in TenantIntegration). Credentials are decrypted at the point of use; the provider-invite worker already loads + decrypts them. EXECUTION RULE: write SOURCE ONLY - no npm/npx/tsc/prisma/docker (orchestrator builds/tests). NodeNext ESM (.js specifiers). HARD RULES: additive + backward-compatible (frozen v1 + current v2 unaffected; no breaking route/response changes); REAL data or honest empty/inert ONLY - an un-keyed vendor shows 'connect your account', a not-yet-taken test shows 'awaiting result', NEVER a fabricated test/score/'connected'; never log/store/return decrypted credentials or secrets; no em or en dashes in authored text; match existing idioms. Stay STRICTLY in your lane's territory.";

phase('Build')
log('2 assessment-friendly lanes')
const out = (await parallel([
  () => agent(CTX + "\n\n=== LANE 1: BACKEND - browse vendor tests + validate connection (territory: apps/assessment-service + apps/api-gateway proxy check ONLY) ===\nGive the UI a way to browse a connected vendor's REAL test library and validate a key, so a recruiter never has to know a raw HackerRank/HackerEarth test id.\n1. READ how the provider-invite worker loads + AES-GCM-decrypts the tenant's TenantIntegration creds for a provider kind (reuse that EXACT decrypt path - do NOT reimplement crypto, do NOT expose secrets). READ providers/index.ts (getProvider) + providers/types.ts (listTests) + how TenantIntegration is looked up per tenant+kind.\n2. Add GET /internal/assessments/providers/:kind/tests (kind in hackerrank|hackerearth|codility|imocha|testgorilla) -> loads the tenant's decrypted creds for that kind, calls provider.listTests(creds), returns [{ id, name, category, durationMinutes }] (NEVER the raw creds). Honest states: 404/409 'not connected' when the tenant has no integration for that kind; surface a clear error (not a 500) when the vendor rejects the key (invalid credentials) so the UI can say 'check your key'. Respect the adapter's rate limits.\n3. Add GET /internal/assessments/providers/:kind/validate -> a lightweight connection check (a listTests or a cheap authenticated call) returning { connected: boolean, testCount?: number, error?: string } - real result, never a fake 'connected'.\n4. Add GET /internal/assessments/providers -> the tenant's assessment integrations with per-vendor { kind, connected, lastValidatedAt? } so the UI can show status. All mounted under the existing /internal/assessments (reachable via the existing /api/assessments gateway proxy - CONFIRM the gateway already proxies /api/assessments to assessment-service; if a sub-path needs a mount, note it, do not break existing proxies).\nReport the exact routes + JSON shapes in contract.", { label: 'af:backend', phase: 'Build', schema: REPORT }),

  () => agent(CTX + "\n\n=== LANE 2: FRONTEND - first-class send-test + results + guided connect (territory: apps/frontend ONLY: the candidate profile assessment section, the assessments pages app/(dashboard)/assessments/*, and settings/integrations/page.tsx; do NOT edit shared lib/api.ts - use a lane-local assessment-api helper) ===\nMake HackerRank/HackerEarth feel native to the hiring flow. Consume Lane 1's new endpoints (GET /api/assessments/providers, /providers/:kind/tests, /providers/:kind/validate) - assume EXACTLY those shapes; add API calls in a NEW lane-local helper file, not shared lib/api.ts.\n1. ONE-CLICK SEND FROM THE CANDIDATE: at the candidate's Assessment stage (candidate profile), add a 'Send coding test' action: pick vendor (HackerRank / HackerEarth, showing connected status), browse the vendor's REAL test library (from /providers/:kind/tests) with an honest 'connect your account' empty state when not keyed, pick a test, and send the invite via the existing POST /api/assessments/:id/invite { provider, providerTestId } flow (reuse however the app currently creates/《resolves》the assessment for a candidate; if an assessment must exist first, wire the minimal existing path). Show the resulting invite status (PENDING -> SENT) + the candidate take link. Never fabricate.\n2. PROMINENT REAL-TIME RESULTS: make the OA results (score, percentage, problems-solved sections, plagiarism flag, report link) prominent on the candidate profile (reuse the existing results panel / GET /api/assessments/:id/results) with honest 'awaiting result' until the vendor reports. Add/または enhance the assessments dashboard (app/(dashboard)/assessments/page.tsx) to list invites + results across candidates, sortable by score, filterable by vendor.\n3. GUIDED CONNECT + TEST CONNECTION: in settings/integrations, for HackerRank + HackerEarth add a 'Test connection' button that calls /providers/:kind/validate and shows a real connected/failed state + test count, and a short inline how-to (where NCR Voyix finds their HackerRank API key / HackerEarth client_id+secret+webhook secret). Keep secret fields write-only/masked.\nUse ONLY plain English in UI copy (no em/en dashes). Report the surfaces changed.", { label: 'af:frontend', phase: 'Build', schema: REPORT }),
])).filter(Boolean)
log('Build done: ' + out.length + '/2 lanes')

return { lanes: out.map(o => ({ summary: o.summary, files: o.files.length, contract: o.contract, notes: o.notes })) }
