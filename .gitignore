node_modules/
dist/
.turbo/
.next/
*.log
.env
.env.local
.env.*.local
coverage/
.DS_Store
*.tsbuildinfo
.idea/
.vscode/
!.vscode/settings.json
/tmp/
.cache/

# Phase 31e — local backup directory (never commit dumps)
/backups/
*.sql.gz

# Demo launcher runtime output (changes every run)
CURRENT-DEMO-URL.txt

# Session recon + design-export artifacts (kept on disk, not committed)
.cloudflared-demo.log.err
.recon/
claude-design/
samples/
docs/CLAUDE_DESIGN_PROMPT.md
docs/DESIGN_KICKOFF_PROMPTS.md
docs/MOTIONSITES_STYLE_PROMPTS.md
docs/UI_INSPIRATION_MAP.md
scripts/audit-frontend-coverage.cjs
scripts/build-design-prompt.cjs
scripts/digest-recon.cjs
scripts/split-recon.cjs

# Prisma platform-specific query engine binaries generated at build/runtime
# (the repo tracks the dev Windows engine; Linux/Docker engines are produced by
# `prisma generate` during the image build and must not bloat the repo).
**/src/generated/prisma/libquery_engine-*.so.node
**/src/generated/prisma/*.dylib.node
