export const meta = {
  name: 'wfab-devui-foundation',
  description: 'WF-A + WF-B: developer-UI customization foundation — typed UiConfig contract (zod + migration) in @cdc-ats/contracts, plus the surface/slot registration API, theme-CSS sink, page codegen, and SDK barrel in apps/frontend. Additive; untouched screens render byte-identical.',
  phases: [
    { title: 'Contract', detail: '1 agent: UiConfig zod schema + migrate + export (contracts)' },
    { title: 'Registry', detail: '3 agents: surface-registry, build-theme-css, codegen+baseline' },
    { title: 'Wire', detail: '3 agents: slots (needs registry), catch-all route (needs codegen), SDK barrel' },
    { title: 'Verify', detail: 'contracts+frontend tsc + run codegen + re-tsc' },
  ],
}

const REPORT = { type: "object", additionalProperties: false, properties: {
  files: { type: "array", items: { type: "string" } }, summary: { type: "string" }, buildClean: { type: "boolean" }, contract: { type: "string" }, notes: { type: "string" },
}, required: ["files", "summary", "buildClean", "contract", "notes"] };

const CTX = "Repo D:/CDC/ATS. Docker not needed; verify with `npm run build --workspace=@cdc-ats/contracts` and `cd apps/frontend && npx tsc --noEmit`. READ the real patterns FIRST and reuse them: apps/frontend/lib/widgets/schema.ts (the migrateDashboard schemaVersion+MIGRATIONS ladder + makeInstanceId), apps/frontend/lib/widgets/registry.ts (the CatalogEntry shape, the filter() at ~498-522 = role+plan+module gate, assertRealSource ~456), apps/frontend/components/cd/cd-shell.tsx (~54-66 buildBrandStyle scoped <style> via .cd-scope[data-cd-brand]), apps/frontend/lib/theme/brand-ramp.ts (hex->oklch ramp), packages/contracts (zod DTOs incl DashboardDocumentSchema), packages/common MODULE_REGISTRY + ModulePlan, hooks/use-modules.ts + use-tenant-branding.ts (fail-soft cached fallback). HARD RULES: every change ADDITIVE + fail-soft (untouched tenant resolves to all-enabled fallback, byte-identical render); NO mock/fabricated data; CSS-injection defense (hex/font fields .parse()d before reaching any inline <style>); no em/en dashes. This is WF-A+WF-B of a 9-workflow program (developer-customizable UI + 10+ hiring integrations + 5k-scale).";

phase('Contract')
log('UiConfig contract in @cdc-ats/contracts')
const contract = await agent(CTX + "\n\n=== WF-A: UiConfig contract (packages/contracts) ===\nCreate packages/contracts/src/ui-config.ts: UiConfigSchema (zod) = { schemaVersion:number, brandName?:string, theme:{ brandHex?:string(/^#([0-9a-fA-F]{6}|[0-9a-fA-F]{3})$/), aiAccentHex?, secondaryHex? (same regex), colorMode:enum['system','light','dark'].default('system'), density:enum['compact','cozy','comfortable'].default('cozy'), radius:enum['sharp','soft','round'].default('soft'), tokens?:record(string), logoUrl?:url, faviconUrl?:url, fontFamily?:string, fontSrc?:url, fluid?:{minViewport,maxViewport,minBase,maxBase,typeRatio,spaceRatio} }, nav:{ order:string[].default([]), hidden:string[].default([]), overrides:record(object({label,icon,href}).partial()).default({}) }, routes:record(object({enabled:boolean.default(true), title?:string})).default({}), copy:record(string).default({}), surfaces:record(object({enabled:boolean.default(true), slots:record(array(SlotBindingSchema)).default({})})).default({}), featureToggles:record(boolean).default({}) }. SlotBindingSchema = { componentId:string, order?:number, roles?:string[], requiredModule?:string, planTier?:string }. export type UiConfig = z.infer<...>; export SlotBinding type. Add migrateUiConfig(doc:unknown):UiConfig — a schemaVersion ladder COPIED VERBATIM from lib/widgets/schema.ts migrateDashboard (CURRENT_SCHEMA_VERSION=1 + MIGRATIONS[] + version-walk + final .parse). All hex/font values are validated by the schema (.parse) so they are safe to inject. EDIT packages/contracts/src/index.ts to `export * from \"./ui-config.js\"`. Build: npm run build --workspace=@cdc-ats/contracts. In `contract` give the exact exported names + the UiConfig shape.", { label: 'a:contract', phase: 'Contract', schema: REPORT })
log('Contract done: buildClean=' + (contract && contract.buildClean))

phase('Registry')
log('surface-registry + theme sink + codegen baseline')
const reg = (await parallel([
  () => agent(CTX + "\n\n=== WF-B/B1: surface registry ===\nCreate apps/frontend/lib/registry/surface-registry.ts: a SurfaceEntry type { id:string (stable, e.g. 'page:candidates','widget:kpi_scorecard'), kind:'page'|'widget'|'slot', component:()=>Promise<{default:React.ComponentType<any>}> (lazy loader), roles:string[], requiredModule?:string, planTier?:string, defaultConfig?:Record<string,unknown>, inputs?:InputSpec[] }. InputSpec = { name:string, type:'string'|'number'|'boolean'|'color'|'enum'|'file'|'list'|'object', defaultValue?:unknown, friendlyName?:string, options?:{label:string;value:string}[] }. registerSurfaceComponent(SurfaceEntry) + registerWidget (re-export/wrap the existing widget registry's register if present) sharing ONE gate shape {roles,requiredModule,planTier}; REUSE the EXACT filter() logic from lib/widgets/registry.ts (role+plan+module) — import it or replicate it identically. validateSurfaceRegistry() fail-fast that copies the assertRealSource invariant idea (a surface bound to a non-real data source throws). In-memory registry Map keyed by id. In `contract` give the SurfaceEntry + InputSpec + the register/filter API.", { label: 'b1:registry', phase: 'Registry', schema: REPORT }),
  () => agent(CTX + "\n\n=== WF-B/B5: build-theme-css ===\nCreate apps/frontend/lib/theme/build-theme-css.ts: lift + GENERALIZE the buildBrandStyle logic from cd-shell.tsx (~54-66) into a pure function buildThemeCss(cfg: { brandHex?, aiAccentHex?, secondaryHex?, colorMode?, density?, radius?, fontFamily?, fontSrc?, fluid? }, scopeAttr='data-cd-brand'): string that returns a scoped <style> body for .cd-scope[data-cd-brand] emitting ALL token overrides: the brand ramp (reuse lib/theme/brand-ramp.ts), an AI-accent ramp (same ramp fn on aiAccentHex), an optional @font-face (fontSrc) + --font-sans override, --radius/--density data-driven values, and optional Utopia clamp() --fs-*/--space-* from fluid{}. Honor colorMode 'system' (emit both light + .dark var blocks). Pure, no React, hex/font already-validated by the caller. Do NOT yet edit cd-shell or embed-shell (that is WF-C). In `contract` give buildThemeCss signature + what tokens it emits.", { label: 'b5:theme', phase: 'Registry', schema: REPORT }),
  () => agent(CTX + "\n\n=== WF-B/B3: page codegen + baseline ===\nNext.js next/dynamic CANNOT take a variable import path, so registered PAGES need a build-time literal-import map. Create apps/frontend/scripts/gen-surface-registry.ts: a Node/tsx script that scans the surface registrations (for now, a documented convention: it reads a registrations manifest or the registry module) and EMITS apps/frontend/lib/registry/generated.ts with literal `import()` entries keyed by surface id. Create apps/frontend/lib/registry/generated.ts as a checked-in BASELINE (an empty `export const GENERATED_SURFACES: Record<string,()=>Promise<{default:React.ComponentType<any>}>> = {};`) so tsc passes before codegen runs. The script must be idempotent + safe to run in the Docker builder before next build. In `contract` give the generated.ts shape + how to run the script.", { label: 'b3:codegen', phase: 'Registry', schema: REPORT }),
])).filter(Boolean)
log('Registry done: ' + reg.length + '/3')

phase('Wire')
log('slots + catch-all route + SDK barrel')
const wire = (await parallel([
  () => agent(CTX + "\n\n=== WF-B/B2: slot system ===\nThe Registry phase created lib/registry/surface-registry.ts (SurfaceEntry, registerSurfaceComponent, the shared filter()). Create apps/frontend/lib/registry/slots.tsx: a CLOSED typed SlotId union = 'shell.header.right'|'shell.nav.footer'|'candidate.detail.before'|'candidate.detail.sidebar'|'requisition.detail.actions'|'screening.verdict.footer'|'dashboard.toolbar'; a <Slot id:SlotId ctx?:unknown/> component that reads registered SlotBinding[] for that id, FILTERS them with the SAME filter() (role+plan+module) from surface-registry, layers any per-tenant config.surfaces[route].slots binding on top, resolves override-by-id (a binding for a stable id replaces the default; NEVER eject/fork), and renders each bound component lazily with the typed ctx prop. Fail-soft: unknown/empty slot renders null. Import SurfaceEntry + filter from B1. In `contract` give the SlotId union + <Slot> props + override precedence.", { label: 'b2:slots', phase: 'Wire', schema: REPORT }),
  () => agent(CTX + "\n\n=== WF-B/B4: catch-all ext route ===\nCreate apps/frontend/app/(ext)/[...slug]/page.tsx: a NEW route group (zero overlap with (dashboard)/(embed)/(candidate-portal)) catch-all that maps the slug -> a registered SurfaceEntry of kind 'page' -> renders it via next/dynamic using the GENERATED_SURFACES literal-import map from lib/registry/generated.ts (WF-B/B3). 404 (notFound()) when no surface matches or its role/module/plan gate fails. Client/server split as needed (dynamic ssr:false for client-only surfaces). In `contract` give the route behavior + how it resolves a slug to a generated import.", { label: 'b4:route', phase: 'Wire', schema: REPORT }),
  () => agent(CTX + "\n\n=== WF-B/B6: SDK barrel ===\nCreate apps/frontend/lib/registry/index.ts: the developer SDK barrel re-exporting registerSurfaceComponent, registerWidget, the Slot component + SlotId type, InputSpec types, and a forward-declared useUiConfig (re-export from lib/config/ui-config-provider if it exists, else a typed stub `export const useUiConfig = ...` to be wired in WF-C — make it tsc-clean now, e.g. re-export a typed no-op that returns the all-enabled fallback). Disjoint file. In `contract` give the public SDK surface.", { label: 'b6:barrel', phase: 'Wire', schema: REPORT }),
])).filter(Boolean)
log('Wire done: ' + wire.length + '/3')

phase('Verify')
const verify = (await parallel([
  () => agent("Build verification for D:/CDC/ATS. Run verbatim and report each: npm run build --workspace=@cdc-ats/contracts ; node apps/frontend/scripts/gen-surface-registry.ts ; cd apps/frontend && npx tsc --noEmit. REPORT: files=any with errors, summary=pass/fail, buildClean=all green, contract='', notes=every error verbatim + whether codegen ran clean.", { label: 'vf:build', phase: 'Verify', schema: REPORT }),
  () => agent(CTX + "\n\n=== AUDIT: additive + fail-soft + no-fabrication (read-only) ===\nVerify citing file:line: (1) UiConfigSchema fields default to empty/optional so an untouched tenant resolves to an all-enabled fallback (no invented theme/nav); migrateUiConfig mirrors migrateDashboard. (2) surface-registry reuses the EXACT widget filter() gate; validateSurfaceRegistry carries the real-source invariant. (3) <Slot> renders null for empty/unknown ids; override-by-id never forks. (4) the (ext) catch-all uses the literal generated-import map (no variable next/dynamic path) and 404s on gate failure. (5) NO existing screen behavior changed (cd-shell/embed-shell NOT edited yet); no mock data; hex/font validated before any <style>. Return REPORT: files=violations, summary=verdict, buildClean=true iff clean, contract='', notes=each issue file:line.", { label: 'vf:audit', phase: 'Verify', schema: REPORT }),
])).filter(Boolean)

return { contract, reg, wire, verify }
